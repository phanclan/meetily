//! Meetnola default preference helpers.
//!
//! Full defaults flip (Groq → local STT, etc.) is intentionally out of scope
//! for the plugin extraction; keep this module as a thin placeholder.

/// Placeholder for future Meetnola default preference application.
#[allow(dead_code)]
pub fn apply_meetnola_defaults_placeholder() {
    // no-op — defaults flip lands in a later task
}
