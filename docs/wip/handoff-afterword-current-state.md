# Afterword Current State Handoff

Last updated: 2026-09-09

> **Rename note.** This fork is now called **Afterword** (previously Meetnola). Code,
> scripts, Cargo features, the Tauri plugin namespace, and build env vars all use
> `afterword`. The bundle id stays `com.meetnola.tester` so app data and macOS TCC
> grants survive. Historical paragraphs below keep their original wording, including
> `/private/tmp/meetnola-*` log paths, past build IDs, and note titles such as
> **Meetnola Luna long-source review** that literally exist with those names. Only
> commands and current-state facts were updated.

Current checkout: local `main`. Continue work directly on `main`; branches and worktrees require an explicit request. Nothing was pushed. Earlier branch references below describe historical validation state.

Activated with Peter’s approval: dev build `20260909-afterword-events` now runs from `/private/tmp/meetnola Dev.app` with the native **Afterword** window and **Afterword Dev** application name. The executable and bundled ICNS were replaced together, retaining `com.meetnola.tester`, data location, audio entitlements, and frontend hot reload on port 3118. Ad-hoc signature verification passes; bundled icon checksum matches the source. The build includes completion events, Afterword branding, and the device-monitor fix. The previous complete app bundle is preserved at the path in `/private/tmp/afterword-activation-backup-path`. All 58 notes remain. The existing synthetic **Afterword draft resume check** note was enhanced through the saved Luna configuration; finished text displayed correctly, and the frontend log recorded `fetchMs: 2` and `afterSavedEventMs: 3`. No microphone recording or physical device-disconnect test occurred. Native build and TypeScript passed; log files are `/private/tmp/afterword-activation-{build,types}.log`. The standalone Applications tester was untouched; no commit or push occurred. Resume frontend hot reload after this batched activation. Earlier paragraphs below describe their pre-activation state.

Enhancement completion update (uncommitted on `main`): successful native persistence now emits `summary-completed`; the frontend immediately reads saved data, with initial/subscription-gap reads and five-second recovery polling. New regressions cover in-flight completion, duplicate/unrelated events, missed events, late registration cleanup, observer replacement, and elapsed-time timeouts. All 172 frontend workflow tests, TypeScript, and 27 scoped native summary-service tests pass. An isolated browser using the production observer, generation hook, and BlockNote editor displayed identical synthetic notes through both paths: saved-result-to-rendered-summary-state was 31 ms with events and 4,912 ms with events suppressed. This is a synthetic UI measurement, not a live provider or native delivery benchmark. Logs: `/private/tmp/afterword-summary-events-{focused,workflows,types,native,preview}.log`; fixture pointer: `/private/tmp/afterword-summary-events-fixture-path`. Native compilation was for tests only; the running app remains on `20260909-luna-chat`, so event delivery awaits the next batched native activation. No model request, recording, commit, or push occurred.

Commit verification for the accumulated Afterword baseline: the current native suite passes 285 tests with 12 opt-in evaluations skipped; the isolated production frontend export also passes with `NEXT_PUBLIC_FLAVOR=afterword`. Logs: `/private/tmp/afterword-precommit-native.log` and `/private/tmp/afterword-precommit-frontend.log`. The preceding Home pass passed all 167 frontend workflow tests and TypeScript. Generated browser-test output and the local package cache are ignored. The native app remains on the existing executable with hot reload; production compilation does not activate native branding or the device-monitor fix. Earlier uncommitted statements below describe the state at their respective checks.

Current working mode: Peter chose frontend hot reload; leave the native executable and permissions unchanged. Home now hides empty drafts, shows resumable title/content/pending-save drafts above the library, and labels the header **Resume draft** when appropriate. Note-row menus reuse the incumbent Radix component with keyboard navigation, menu/dialog focus handoff, visible narrow-screen triggers, and search focus after Trash. Native **Afterword draft resume check** (`meeting-note-6e518a7d-bff3-40a4-a433-36a38d4574be`) verified draft title/Unicode preview, resume, save, and removal of the card after success; 58 notes remain, app idle on Home. Organizer keyboard selection and Escape/focus return passed natively. An isolated preview of the production component covered empty/title-only/pending drafts, synthetic Trash, and zero horizontal overflow at 320, 390, and 960px. All 167 workflow tests, final TypeScript, and diff checks pass; logs: `/private/tmp/meetnola-home-polish-{workflows,types,preview}.log`. Temporary preview tabs/server were closed. No recording, model call, native rebuild, commit, or push occurred.

Concurrent Afterword branding changes from the user's other task were preserved. **About Afterword** is visible through hot reload; the native window/build badge still identify the existing `20260909-luna-chat` binary. Native branding and the pending device-monitor fix require a later batched activation, consistent with the hot-reload decision.

Recording startup clarification: the user identified OS microphone approval as a possible contributor to the earlier delay. Frontend logs show model preflight completed in milliseconds, then 68.0 seconds elapsed between native start invocation and the recording-started event; they do not separate permission waiting from native initialization. A fresh launch of the unchanged `20260909-luna-chat` binary started in 3.45 seconds and stopped/saved zero transcript segments. Do not present the earlier duration as an established performance defect. The dev app is ad-hoc signed with a hash-based designated requirement, and `security find-identity -v -p codesigning` returned zero valid identities. Repeated native replacements explain renewed permission prompts; prefer frontend hot reload and batch native updates until stable development signing is configured. The running binary has not been replaced in this pass.

A process sample taken after the fresh startup exposed synchronous device discovery on a Tokio worker. The device monitor also calculated but never applied its healthy five-second interval. The pending fix moves discovery to the blocking pool, applies the existing five-second healthy/two-second missing cadence, allows Stop to leave an in-flight scan without late events, and avoids leaving a stop notification when already idle. Five focused native tests pass, including virtual-clock cadence/disconnect behavior, cancellation during stalled discovery, and idle-stop reuse. Native build and diff checks also pass; build log: `/private/tmp/meetnola-recording-monitor-build.log`. These changes are not yet activated or verified with real device disconnects. The app remains idle on Home with 57 notes, including **Meetnola recording startup check**. Evidence: `/private/tmp/meetnola-startup-sample.txt` and `/private/tmp/meetnola-recording-monitor-tests.log`. No model call, permission reset, commit, or push occurred in this pass.

Native import-to-export verification passed on `20260909-luna-chat`: a 94-second synthetic WAV imported through **Import audio** with Parakeet Compact as **Meetnola Cedar export workflow** (`meeting-4e63de69-7d93-47e5-9458-0c80d88d9218`). All four transcript segments, including the closing passage at 1:18, appeared in the source drawer. The saved Gateway/Luna configuration generated enhanced notes that preserved confirmed assignments, proposal/completion distinctions, and the original Unicode note. **Meeting actions → Export Markdown** used Documents without a custom-folder change. The created file `/Users/peterphan/Documents/20260910-032525-Meetnola-Cedar-export-workflow-4319f5ea-4984-4fc6-9282-06cda86d1b6a.md` was read back and contains metadata, enhanced notes, original café/日本語 text, and all four timestamped transcript segments. Fixture: `/private/tmp/meetnola-export-workflow-_qtwxu24/cedar-spoken.wav`. This verifies one synthetic audio import and manual export, not live microphone startup, long-recording reliability, or general model accuracy. No credentials were read, no notes deleted, and nothing committed or pushed.

Current chat accuracy result: the combined final-status instructions, current sources after history, and medium reasoning corrected the observed disputed-assignment failure in a native rerun. **Meetnola Luna long-source review** retained its source and prior incorrect answers; the latest response lists only Rosa/Wednesday, Mateo/Friday, and Kim/Thursday under confirmed work, preserves both briefing versions under unresolved matters, and retains the editable-notes requirement. The two earlier low-reasoning prompt trials (`20260909-chat-fidelity` and `20260909-chat-final-status`) both failed this same grouping check. The source builder includes the full written text; no truncation was found there. This is one diagnostic rerun with accumulated conversation history, not an independent benchmark or proof of general factual accuracy.

The control in **Meetnola Luna correction and checklist review** also passed: Lee/Thursday remains the explicitly resolved assignment, both source checkboxes are recognized as complete, and the fenced example is excluded from tasks. Both answers completed within the existing 2,048-token response limit and persisted after full Quit/relaunch. A subsequent cited follow-up correctly resolved “that remaining draft” to Lee/Thursday. Active, signature-verified dev build: `20260909-luna-chat`; prior executable: `/private/tmp/meetnola-before-luna-chat`. Eleven native request regressions pass, including the chat-only reasoning boundary and existing cancellation/stream tests; final native build and diff checks pass. Logs: `/private/tmp/meetnola-chat-medium-final-tests.log` and `/private/tmp/meetnola-luna-chat-build.log`; trial logs retain the `meetnola-chat-*` stems. Enhancement and source review retain low reasoning. The Mac lock was cleared; the app is idle on the latest long-note conversation with 55 notes. No source notes, model selection, or credentials were changed; no recording, commit, or push occurred. Real-meeting quality and latency remain unbenchmarked.

Gateway chat streaming was verified in dev build `20260909-gateway-stream` and remains enabled in the active chat trial above; prior executable: `/private/tmp/meetnola-before-gateway-stream`. Luna now uses the incumbent text-stream decoder and chat delta channel. Enhancement and source review remain complete-response requests. Reasoning/usage metadata stays out of displayed answers; incomplete or failed streams retain their existing labels and do not become completed follow-up history. Validation: 127 native summary tests passed with eight opt-in tests ignored, followed by one new socket-cancellation test and four frontend stream/persistence regressions. Native build and diff checks pass. Logs: `/private/tmp/meetnola-gateway-stream-{tests,cancel,ui-tests,build}.log`.

Native Gateway streaming verification used only **Meetnola Luna long-source review**. One cited answer completed; a second showed multiple useful paragraphs while **Stop answer** remained visible. Stop retained that partial with “Stopped. This answer is incomplete.” and no later completion replaced it. A source button opened the written-note excerpt. After full Quit/relaunch, the completed answer, stopped text, notice, and citation controls all persisted. No recording, credential inspection, note deletion, commit, or push occurred. This proves the observed streaming workflow, not inference speed or factual accuracy. That completed chat answer exposed the disputed-assignment issue subsequently addressed in the diagnostic checks above. The enhancement result remains unchanged and passed its earlier control.

Quick-note checkbox correction: the shared plain-text conversion now creates source checkbox blocks from explicit Markdown task items, retaining checked state. Quoted, fenced, indented-code, and non-task examples remain paragraph text. This covers standalone saves, recording-draft seeding, and Markdown-only note recovery without migrating saved JSON. All 167 workflow tests and TypeScript pass; logs are `/private/tmp/meetnola-draft-checkbox-{workflows,types,focused}.log`. Native **Meetnola Luna correction and checklist review** (`meeting-note-5c28b54a-39c4-4778-8145-5bc799f6a23f`) verified a pasted open checkbox in Open, a pasted checked item in Completed, and a fenced example excluded from both. Completing the open task updated its original block. After full Quit/relaunch, both completed states, the original source, title, and enhanced result persisted; the source link retained Completed and opened My notes. Recording-draft seeding was not exercised natively in this check. Nothing was committed or pushed.

Latest Luna accuracy check: **Meetnola Luna long-source review** (`meeting-note-a72ac6b7-fb57-401a-9022-c1a646c2bd14`) contains the complete 13,060-character `varied-release-review` fixture as one written note with labeled written notes and transcript excerpt. This differs from the separate-transcript CLI transport, so it is not an identical Gemma comparison. The baseline preserved the three confirmed technical assignments and core policy facts, but incorrectly confirmed Lee/Thursday for the unresolved briefing and omitted the requirement to keep written notes editable. After the instruction correction, a native rerun of this unchanged source preserved the editable-notes requirement, reported Alex/Tuesday versus Lee/Thursday as unresolved, and listed only Rosa/Wednesday, Mateo/Friday, and Kim/Thursday as confirmed tasks. The short correction control above correctly accepted Lee/Thursday after an explicit resolution, retained Morgan/Friday, and excluded a completed review and quoted example from new tasks. Both reports persisted across full Quit/relaunch. Evidence: `/private/tmp/meetnola-luna-long-review.json`. These two single synthetic runs support the observed fixes, not general long-meeting accuracy or the entire Granola replacement goal.

Previously activated dev build `20260909-luna-fidelity` strengthened the incumbent report instructions for unresolved source disagreements and written requirements; these changes remain in the current streaming build. Translation-cache identity now includes those instructions, rejecting older cached reports after rule changes. Processor and service suites each pass 27 tests; native build and diff checks pass. Logs: `/private/tmp/meetnola-luna-fidelity-{native-tests,cache-tests,build}.log`; prior executable: `/private/tmp/meetnola-before-luna-fidelity`. The Mac lock was cleared and native validation completed as recorded above. No audio was captured, no credentials were read, and the standalone tester was untouched. Changes remain uncommitted on main; nothing was pushed. Broader model evaluation and real recording reliability remain unproven.

Gateway native verification: the user entered a Gateway key, and the Luna configuration was saved through Settings. Provider `custom-openai`, endpoint `https://ai-gateway.vercel.sh/v1`, model `openai/gpt-5.6-luna`, and the masked credential persisted across Quit/relaunch. This connection check ran on `20260909-followups-gateway-check`, since superseded by the active build above; backup is `/private/tmp/meetnola-before-followups-gateway-check`. Test Connection returned a complete response. Its result now remains visible in Settings and clears when configuration changes; TypeScript passes after this frontend addition. Parakeet Compact remains selected and no audio was captured.

Native synthetic note **Meetnola Luna Gateway verification** (`meeting-note-e3783c66-14fd-4eea-9aa4-b2ca180e23c1`) verified real Gateway chat and enhancement. Luna correctly identified Morgan's Friday commitment, Monday as an unagreed proposal, and the briefing owner as unassigned after Lee declined; enhancement retained the timestamp/speaker export requirement. A source-note checkbox appeared in Follow-ups, completion updated the original note, and the source link opened My notes with Back to follow-ups retaining Completed. A full second restart preserved the custom title, enhanced result, question/answer, exact S1 snapshot, and checked source block. This is one synthetic functional/quality check, not a long-meeting quality benchmark or proof the broader Granola replacement goal is complete. At that check the app had 53 notes; the subsequent long-source check is recorded above. No credentials were read or printed, no notes deleted, and nothing committed or pushed.

Gateway connection validation hardening: Settings now applies the same complete-text checks used by enhancement/chat, rejecting empty, reasoning-only, filtered, truncated, or malformed responses. HTTP failures report actionable authentication, credit, permission, missing-model, rate-limit, or availability messages without forwarding provider response bodies to frontend logs. Two focused regressions pass. General settings now distinguish local saved data from text sent to selected cloud enhancement/chat providers. Isolated production export with `NEXT_PUBLIC_FLAVOR=afterword`, native build, and diff checks pass; logs are `/private/tmp/meetnola-gateway-connection-tests.log`, `/private/tmp/meetnola-gateway-followups-production.log`, and `/private/tmp/meetnola-gateway-followups-build.log`. That build is now activated and native acceptance is recorded above.

Gateway/Luna switch in progress: the existing Custom OpenAI preset now uses `openai/gpt-5.6-luna` at `https://ai-gateway.vercel.sh/v1`. Vercel's public catalog and reasoning documentation confirm the ID and low-effort support. Summary and question requests share a Gateway-only low-reasoning profile; Luna questions allow 2,048 output tokens, and the connection test allows 512. Preset application clears credentials from a different endpoint and resets inherited sampling settings. Removed config/key-bearing console logs from settings and their consumers. Eight focused native request tests, TypeScript, native build/signature, and diff checks pass. Active dev build is `20260909-gateway-luna`; backup is `/private/tmp/meetnola-before-gateway-luna`. Key entry and live synthetic validation subsequently completed, as recorded above. No credential was read or printed. Parakeet transcription is unchanged.

Follow-ups implementation and synthetic validation: the Home entry opens original written-note checkboxes with Open/Completed and folder filters, paging, completion/reopening, and source-note navigation that retains folder/status and requests My notes. Native CAS saves preserve concurrent edits and reject unavailable or trashed notes. Legacy string content, nested links, malformed blocks, and unusual checked values no longer break listing; plaintext preserves checkbox status and inline spacing. Three native tests, 165 frontend workflow tests, TypeScript, native build, and diff checks pass. The production route/hook/write helper were exercised against synthetic IPC in a separate browser preview: complete/reopen, folder-empty state, source URL, desktop layout, and 320px wrapping pass. Preview tab/server were closed and viewport reset. Fixture: `/private/tmp/meetnola-followups-ceq3nix4`; logs: `/private/tmp/meetnola-followups-{native,workflows,types,build}.log`. The Follow-ups build was subsequently rebuilt with connection-check hardening; see the newest build ID above. It is now installed into the dev app; native source-note completion and live Luna validation are recorded above. Changes are uncommitted on main; nothing pushed.

Latest recording-state fix: `RecordingStateContext` now serializes polls, rejects responses older than lifecycle/events, polls during Starting, restores RECORDING on backend confirmation after reload, and preserves processing/saving while clearing confirmed stopped capture flags. Late listener registrations are released after unmount. The status setter is stable, identical snapshots reuse state, and per-poll logging is removed. Five delayed-response regressions pass; all 162 workflow tests, TypeScript, production static export, and diff checks pass. Native dev check reached Recording live with Stop visible in 0.74 seconds (start request to started event), stopped normally, and saved zero transcript segments as **Meetnola recording state verification**. Subsequent HMR stayed Not recording. This warm check does not explain or prove resolution of the earlier 66-second startup delay; its exact cause remains unconfirmed. Changes are frontend-only on active binary `20260909-recording-chat-history`, uncommitted on main, with nothing pushed. Logs: `/private/tmp/meetnola-recording-state-{workflows,types,static}.log`. No notes were deleted; no model change or model request occurred in this pass.

Latest continuity fix: recording questions now reuse `usePersistentChat` with a recording scope instead of ephemeral `useLiveMeetingChat`. A native `recording_chat` staging/mapping table attaches the conversation inside `TranscriptsRepository::save_transcript`, including retries, tray Stop, and recovery. Writes take a lock before resolving the destination, so answers completing after Stop reach the saved meeting. Promotion drops duplicate text and does not overwrite newer saved history. Recording and saved views share the ordered write queue; navigation waits for the final live checkpoint. Empty scratchpads do not read/write history, failed history loads block sending, and new recordings reset ownership without clearing the previous conversation. Recovery deletion clears only unattached chat. Content-free identity markers prevent late writes from recreating deleted chat.

Validation: 271 native tests pass, with 12 opt-in tests ignored; all 157 workflow tests, TypeScript, production static export, build/signature verification, and diff checks pass. Tests cover exact source preservation, post-Stop writes, save retry, failure rollback, deleted/discarded ownership, empty identity, and the navigation checkpoint race. Activated dev build: `20260909-recording-chat-history`; prior executable: `/private/tmp/meetnola-before-recording-chat`. Native **Meetnola recording chat persistence** (`meeting-recording-meeting-1789001004625`) asked a synthetic written-note question while recording. Stop saved zero transcript segments. Its custom title, original text, question, answer, and exact S1 source survived full Quit/relaunch/reopen. The app is idle on that saved conversation; 51 notes remain. Startup stayed at Starting recording longer than expected before becoming live; this was not diagnosed and the run does not establish startup/long-recording reliability. No notes were deleted. Model selection and standalone tester are unchanged. Logs: `/private/tmp/meetnola-recording-chat-{native-final,workflows,types,static,build}.log`. All changes remain uncommitted on main; nothing pushed. Factual summary/task classification remains unresolved.

Latest workspace design pass: live and just-saved quick notes now reuse `MeetingAssistantDock` instead of two custom assistant panels. The permanent transcript/copilot column is removed, with a centered editor, sticky recording controls, serif title, Note actions menu, latest-passage preview, and a shared full-transcript drawer. Live questions now use `buildMeetingAnswerContext` for inspectable source snapshots; existing recipe scopes are preserved. Chat persistence is unchanged (session-only here).

Validation: all 155 workflow tests, TypeScript, production static export, and diff checks pass. The isolated production-page fixture used 205 synthetic transcript passages with no microphone, saved data, or model access. At 1024×576 the editor measured 704px wide; at 390×740 and 320×560 there was no horizontal overflow. Stop remained visible with the assistant expanded. Verified full transcript through passage 205, Escape/focus return for transcript and menu, exact S206 citation text, cancellation from the collapsed assistant, and conversation preservation after synthetic Stop. Native live/saved rendering was visually checked on build `20260909-recent-meeting-sources`. A brief recording saved zero transcript segments; its title **Meetnola live workspace review** appears in the library. A later hot reload stayed idle. The app is on Home with 50 notes, no recording. No notes were deleted, and the selected model/native binary/standalone tester are unchanged. This is layout and interaction validation, not long-recording or factual-answer acceptance. Logs: `/private/tmp/meetnola-workspace-{workflows,typecheck,static}.log`. Changes remain uncommitted on main; nothing pushed.

Latest native recording fixes: reproduced a completely blank live and saved quick-note page while its controls/transcript were present in accessibility. Removing the root Framer Motion zero-opacity entrance animation made the page immediately visible. That hot reload also reproduced a second bug: the old `fresh` URL token was replayed after the saved state reset, starting a new recording. It was stopped and saved zero transcript segments. `consumeQuickNoteStartToken` now records a monotonically increasing consumed token in sessionStorage before any native start request; fresh module loads/Back cannot replay it within the same app-window session. Explicit new starts remain possible after a clock rollback. Guard read/write failures prevent capture. The native active-session check remains in place.

Validation: all 155 workflow tests, TypeScript, production static export, and diff checks pass. Native recording controls were visible during a new short recording. After Stop, a real hot reload of the same consumed URL showed **Not recording** and did not start capture. Command-R alone did not establish a page reload; the hot-reload observation and fresh-module regression are the relevant evidence. The original spoken test saved two transcript segments. Its custom title **Meetnola recording persistence review**, original written verification note, and both transcript segments survived full Quit/relaunch/reopen. ID: `meeting-recording-meeting-1788998729264`. The recording included brief ambient microphone input before the synthetic speech; no recorded content was sent to a model during this check. Two additional zero-transcript QA notes remain: **New note** at 5:07 PM (replay reproduction) and **Meetnola replay guard verification** at 5:11 PM. No notes or audio were deleted. The app is idle on the saved spoken-test note.

These frontend fixes are active through the existing dev server on native build `20260909-recent-meeting-sources`; the native executable, model selection, and standalone tester did not change. Logs: `/private/tmp/meetnola-live-{replay-tests,visibility-typecheck,visibility-static}.log`. Changes remain uncommitted on main; nothing pushed. The root-animation fix is a rendering correction, not a broad redesign, and this short recording check does not establish long-meeting or crash-recovery reliability.

Structured task extraction was also investigated, with no app change retained. Exact quotations prevented fabricated quote text, but the initial extractor mislabeled promises as proposals. Renaming the status to commitment improved that distinction while still losing a separate task under an ownership conflict. Requiring one result per sentence retained the passages but did not fix semantic classification: Gemma promoted a refusal; Qwen 9B incorrectly attached Morgan's independent task to the checklist conflict. Individual-statement queries still returned wrong target IDs, invalid references, and incorrect statuses. Reports and scripts are `/private/tmp/meetnola-task-{extract-probe,extract-commitment,passage-probe,passage-qwen9,targeted-probe}.{json,py}`; sentence input generation is `/private/tmp/meetnola-task-passages.cjs`. These were direct local OpenAI-compatible probes using synthetic fixtures, not native production-pipeline acceptance. Provenance and complete-ID checks cannot establish correct task status. Do not ship these candidates or reinterpret their transport success as an accuracy pass.

Latest task-classification quality pass: retained ten synthetic controls in `frontend/tests/fixtures/recent-task-quality.json` and the runner `frontend/scripts/eval-recent-tasks.cjs`. It uses the actual recent-source formatter with synthetic retrieval plus the existing native streaming question harness; no saved notes are read. The restored production baseline still promotes acceptance criteria and declined requests into tasks, leaves conflicting assignments unlabeled, and in the new availability-check control omits both the explicit tomorrow deadline and Nina's separate Thursday commitment. A valid citation does not establish correct classification or completeness. Conditional commitments and tasks without a named owner are positive controls, so a future fix must not simply reject them.

Four candidates were rejected: a stricter system instruction, default Gemma reasoning with a 2,048-token budget, a question requesting separate categories, and that categorized question with reasoning. None resolved the classification failures without omissions or incorrect placement. The eight-case runs took 2.35 seconds for the original baseline, 2.85 for the instruction candidate, 19.01 for reasoning, 3.34 for categories, and 26.37 for categories plus reasoning. These are sequential synthetic runs with cache/output-length differences, not throughput benchmarks. Reports: `/private/tmp/meetnola-tasks-{baseline,candidate,reasoning,categorized,categorized-reasoning}.json`; the ten-case restored-run report is `/private/tmp/meetnola-tasks-retained-baseline.json`. Exit zero is transport success only; every answer was reviewed manually.

Production `llm_client.rs` was restored byte-for-byte to the pre-experiment snapshot; all seven native request/profile/cancellation tests and diff checks pass. The retained runner completed all ten calls and passed syntax validation. No application behavior, model selection, or active native build changed in this pass. Development remains `20260909-recent-meeting-sources`. New work is limited to evaluation fixtures, runner, and documentation, uncommitted on main with nothing pushed. Next extraction work needs explicit source evidence and task status, with checks for omitted genuine commitments; these results do not justify another global prompt/profile change.

Latest recent-meeting assistant work: **Ask your notes → Sources → Recent meetings** reads full saved original notes/transcripts from the five newest eligible meetings in the selected date range. It bypasses keywords, excludes Trash/empty notes, and reports the number reviewed versus available. One database snapshot covers selection, size checks, and every source row. Oversized sets fail without truncation (120,000 estimated bytes including labels or 1,000 source segments). Existing keyword mode is preserved. A migration adds per-conversation source scope, with existing conversations retaining keyword mode. Draft, range, scope, answers, and citation snapshots persist through Quit/reopen.

Validation: 153 frontend workflow tests, 15 targeted native library tests (one live test ignored), TypeScript, production static export, native build, strict signing, and diff checks pass. Coverage includes a commitment in transcript segment 205, date/Trash selection, deterministic five-meeting bounds, Unicode size rejection, empty sources, and durable/archived settings. Initial 60 KB candidate rejected a representative many-segment fixture because source-label overhead counted against the limit; the retained bound matches the existing 120 KB written-note-review scale. Active development build: `20260909-recent-meeting-sources`; previous executable: `/private/tmp/meetnola-before-recent-sources`. Logs: `/private/tmp/meetnola-recent-{workflows,native,typecheck,static,build}.log`.

Native verification conversation `37d7caec-c172-47bf-83d3-92bd182660e1` used the five newest saved synthetic reviews, correctly disclosed 5 of 9 eligible meetings in Last 7 days, and opened the exact title-preservation transcript at 0:45. Scope, date range, synthetic unsent draft, answer, coverage, and citation survived a full app restart. The existing older conversation and its draft were preserved. The UI was visually inspected before sending and with the reopened citation. No audio, saved source notes, selected model, or standalone tester was changed. Work remains uncommitted on main; nothing pushed. This resolves keyword-only retrieval for bounded recent reviews, not semantic accuracy: the native answer still characterized some requirements as agreed follow-ups. That classification and source-conflict fidelity remain open.

Product priorities confirmed September 9: fast/accurate/editable meeting notes, reliable recording through reopening, finding tasks and answers across recent meetings, and exporting transcripts plus enhanced notes to a designated folder for Obsidian or other LLM workflows. Accuracy remains unresolved; no model was changed in this pass.

Granola comparison follow-up: two separate synthetic chats in Granola's **Auto** mode tested the unresolved and explicitly corrected migration-checklist fixtures. Granola preserved both conflicting owner/date pairs, the editable-notes requirement, and Morgan's confirmed follow-up. It included the disputed checklist under Action Items but explicitly labeled it unresolved; this is not equivalent to inventing a confirmed assignment. The corrected control retained Sam/Thursday and Morgan/Friday plus the written requirement. These are chat observations, not native enhancement or transcription parity: Granola's documented New note action starts capture, so no note/recording was created. Both synthetic chats remain in Granola; existing meeting content was not edited, and the prompts requested only the supplied synthetic sources. The routed model behind Auto was not identified.

An identical-user-prompt local Gemma probe also preserved the required details in both short controls but still failed the long release-review case. This uses direct local chat requests with reasoning disabled and a 2,048-token answer limit, not the application's enhancement pipeline. Report and exact requests: `/private/tmp/meetnola-matched-chat-probe.json`; runner: the matching `.py` file. A compact production-instruction candidate, retaining the existing template, source wrapper, model, and reasoning profile, passed 13/16 cases in 74.63 seconds and was rejected after manual review: long-case written owners/deadlines and requirements remained missing, and short controls still lost written details. The original prompt was restored. Candidate evidence: `/private/tmp/meetnola-compact-instructions-sep09.json` and `.log`. No production behavior or native build changed in this pass.

The earlier recent-meeting retrieval priority is implemented above. Next quality work should distinguish actual commitments from requirements, completed work, and unresolved assignments using the existing source-linked assistant and evaluation fixtures. Preserve the open summary-fidelity gap; recent retrieval does not resolve or replace that requirement.

Latest model comparison: the unchanged Gemma E4B profile passed 14/16 bounded checks in 82.41 seconds; installed Qwen 9B MXFP8 passed 11/16 in 64.93 seconds; downloaded Gemma 12B MLX with reasoning disabled passed 12/16 in 42.21 seconds; reasoning-disabled Gemma E4B passed 12/16 in 20.20 seconds. Later short-case medians were 4.13, 2.76, 1.71, and 0.82 seconds respectively; long-case times were 14.83, 13.07, 13.46, and 5.23 seconds. Runs were sequential with different load/cache conditions and output lengths, so timings are observations, not controlled throughput rankings. Manual review rejected every candidate: wrong owners, uncommitted work promoted to tasks, missing written requirements/deadlines, or invented time. Gemma 12B's default-reasoning run was stopped after a 58.35-second length-limit failure on the sparse note and a 10.89-second successful second case; it is not a completed suite. Both experimental request-profile edits were removed, and the restored profile regression passes. No production prompt, template, model selection, or active native build changed.

Model evidence: `/private/tmp/meetnola-model-{baseline,qwen9,gemma12,gemma12-direct,gemma4-direct}-sep09.json` and matching `.log` files. `gemma4:12b-mlx` was downloaded successfully from the official Ollama library and remains installed but unselected. Existing E4B and Qwen 9B digests match the official tags (`5a08735df03c`, `19c108424cec`). During the runs, `/api/ps` reported context lengths of 131,072 for loaded E4B and 262,144 for both candidates; this removes the earlier unknown loaded-model context for these particular runs, not the general estimator limitation. Benchmark inputs were synthetic; saved user notes were not submitted or altered. The source-conflict and written-requirement failures still need a solution; faster generation alone is not an acceptable replacement.

Latest export feature: **Meeting actions → Export Markdown** defaults to the current user's Documents folder, as requested, and remembers a custom native folder-picker destination. It creates a new UTF-8 Markdown snapshot containing metadata, enhanced notes, original written notes, and every saved transcript segment. Pending editor writes must succeed first; source reads use one transaction, independent of UI pagination. Temporary-file publication and unique dated names preserve external edits. Missing/unwritable destinations fail visibly; exports are manual, not automatic synchronization. Two synthetic verification exports are retained in `/private/tmp/meetnola-export-verification`. See `docs/afterword-maintenance.md` for operation and older-format limitations.

Export validation: 264 native tests pass with 12 opt-in tests ignored, including full 205-segment coverage, timestamps, missing/trashed/pending notes, unique publication, and long Unicode filenames. The first sandboxed suite failed eight mock-server binds; rerunning with local loopback access passed. All 152 frontend workflow tests, TypeScript, production static export, native build, strict signature verification, and diff checks pass. Native exports of the standalone and transcript-bearing synthetic review notes were inspected, and folder selection survived a full restart. The isolated fixture was stopped and its tab closed with viewport reset. The production dialog in that fixture passed cancellation, error/retry, duplicate-submission blocking, Escape/focus return, and zero horizontal overflow at 320×560 and 390×740. Final active build is `20260909-markdown-export-documents`; Documents was selected through the native picker and verified after restart. The final default-folder change passed targeted native export tests, TypeScript, production static export, native build, and signature checks. The pre-export executable is `/private/tmp/meetnola-before-markdown-export`, with the initial export build retained at `/private/tmp/meetnola-before-export-unicode`. Logs use `/private/tmp/meetnola-export-{native-suite,workflows,typecheck,static,build-final}.log`. No audio was captured, saved note content and model selection were unchanged, and the standalone tester was untouched. Work remains uncommitted on main with nothing pushed.

The same pass corrected buffer-timeout headroom from `Duration::mul_f32(2.0)` to exact integer multiplication. The existing Bluetooth test reproduced 159.999996 ms instead of 160 ms; all native tests now pass. This precision correction was separately activated as `20260909-duration-precision` before export work and is included in the final export build. It does not establish end-to-end recording reliability.

Latest coverage feature: **Meeting actions → Review written-note coverage** compares snapshots of current written notes and enhancement through the configured model. It reuses the native query settings/cancellation path and the canonical Dialog/Button components, without adding editor chrome or modifying documents. Every nonempty written line receives a numbered check; missing/duplicate checks and invented supporting draft quotes are rejected. Displayed evidence is copied from the original passage, not generated by the model. All authored passages, including metadata, are accounted for; the user decides relevance. Operational behavior and limits are in `docs/afterword-maintenance.md`.

Coverage validation: 150 frontend workflow tests, 123 deterministic summary tests (eight opt-in tests ignored), TypeScript, production static export, native build, and strict signature verification pass. The final six nonempty-note live controls passed format/provenance checks in 26.58 seconds total and exposed the long draft's missing Alex/Tuesday values and editability requirement. Semantic limits remain: the incomplete short-title control was flagged for its first-line detail while the explanation mistakenly treated the draft title as satisfying title preservation. Metadata is intentionally included in this full-passage coverage action, unlike the earlier substantive-only audit; fixtures retain the old expectations alongside separate `expected_notes_coverage` guidance. An empty review is never an accuracy verdict. Reports: `/private/tmp/meetnola-notes-coverage-{live,accounted,final,verified,all-passages}.json`; only the last uses the final protocol. Final logs use `notes-coverage-{rust-final,workflows,types-final,static-final,build-final,all-passages}.log` in the same temporary directory.

Activated build `20260909-notes-coverage`; prior executable: `/private/tmp/meetnola-before-notes-coverage`. Native review of **Meetnola standalone note review** completed with no possible omissions and showed its exact original-note snapshot. Closing a second review returned focus to Meeting actions; completion raced the close, so native model-abort timing was not established. Controlled browser cancellation and hook regressions cover delayed registration, in-flight cancellation, stale results after navigation/unmount, missing sources, and retry with fresh edits. Browser checks covered results, source disclosure, empty/error/retry states, Escape/focus, and zero horizontal overflow at 320×560, 390×740, and 960×800. The existing enhancement and custom title were unchanged after restart. An ordinary acceptance-criteria question also returned the correct answer with a written-note citation, verifying the existing query path after its optional review argument was added; that synthetic conversation was retained. No audio was captured, model selection and standalone tester were unchanged, and the isolated browser preview was stopped/closed with viewport reset. Work remains uncommitted on main with nothing pushed; generated-summary accuracy remains unresolved.

Latest context-budget fix: local enhancement now budgets the template, written notes, chat overhead, and answer space together. Extraction, merge batches, final reporting, and translation check complete estimated requests. Merges stop on non-shrinking output or after eight reduction rounds. Written notes are never silently trimmed. Unicode budgeting uses UTF-8 bytes for non-ASCII characters, and chunking precomputes offsets instead of repeatedly scanning the full transcript. Explicit Ollama `num_ctx` values take precedence over larger architectural maxima. The estimator is not a tokenizer; the current server reports a 131,072-token architectural context without an explicit allocation, so that value is not verified allocated capacity. Operational limits are in `docs/afterword-maintenance.md`.

Context validation: all 121 deterministic summary tests and nine metadata tests pass; eight opt-in summary tests were ignored. Mock HTTP regressions cover complete request sizes, written-note preservation, Unicode source coverage, bounded merging, non-shrinking output, and oversized translation rejection. The live synthetic long case at forced context 4,000 stopped on truncated extraction after 14.52 seconds, returning no partial report. At reported context 131,072 it completed in 18.05 seconds but still omitted Alex, Tuesday, and editability and assigned the disputed briefing to Lee. These runs validate completion/error paths, not improved factual accuracy. Logs and reports: `/private/tmp/meetnola-context-{tests,metadata-tests,small-window-sep09,reported-window-sep09,build}.log`; live JSON reports use the matching small/reported-window stems.

Activated dev build `20260909-context-budget` passed native build and strict signature verification. Previous executable: `/private/tmp/meetnola-before-context-budget`. Native enhancement of only **Meetnola standalone note review** completed; after app restart, its original written notes, custom title, new result, and displaced previous enhancement were verified. Model selection remains `gemma4:e4b-mlx`; no audio was captured, and the standalone tester was untouched. No frontend code changed in this pass. Changes remain uncommitted on main with nothing pushed; factual fidelity remains the next unresolved product gap.

Latest fidelity comparison: after correcting two scoring false alarms, a fresh baseline passed 12/16 cases in 75.10 seconds total; generating each section separately passed 11/16 in 224.02 seconds; requesting [Google's Gemma defaults](https://huggingface.co/google/gemma-4-E4B-it/blob/main/generation_config.json) (`temperature=1.0`, `top_p=0.95`, `top_k=64`) passed 14/16 in 70.90 seconds. These are single synthetic runs, not general rankings or proof of runtime speed. Neither candidate was shipped: separate sections produced a 998-word long report with a false briefing assignment, while the sampling candidate omitted the written requirement and source conflict and assigned the briefing to Lee. Both experiments were removed; production prompts, sampling, model selection, and active `20260909-note-trash` build are unchanged. Granola's [enhancement documentation](https://docs.granola.ai/help-center/taking-notes/ai-enhanced-notes) makes raw-note preservation and source inspection part of the workflow; preserving typed requirements and unresolved source differences remains the relevant quality gap.

Retained changes: the evaluator accepts the observed valid source-data paraphrase and an exact, reviewed negation of removing all webhooks. Paired regressions still reject actual data loss and a separate contradictory webhook instruction. A new offline rescore command applies current checks to saved outputs while preserving original failures. All 116 deterministic summary tests pass, with eight opt-in tests ignored; the three offline rescoring runs pass. Corrected reports: `/private/tmp/meetnola-{sections-baseline,sections-candidate,gemma-defaults}-rescored-sep09.json`; corresponding originals retain their initial scoring. Test log: `/private/tmp/meetnola-fidelity-scoring-tests-sep09.log`. Changes remain uncommitted on main, with nothing pushed. The optional acceptable-latency question is still unanswered; no implementation is blocked on it.

Trash and restore: Home's immediate deletion is now **Move to Trash**, with a separate **Trash** dialog for restore and confirmed permanent deletion. The tombstone migration preserves note content, summaries/history, folders, and recording paths while excluding trashed entries from the library, folder counts, discovery, legacy transcript search, and new assistant source searches. Restoration is retryable; permanent deletion requires a Trash entry and retains the existing transactional cleanup and library-answer redaction. No automatic expiry or recording-file deletion was added. Procedures and limits are in `docs/afterword-maintenance.md`.

Trash validation: seven native tests pass against isolated SQLite fixtures, covering retained data, retries, failure rollback, enhancement-job guards, search/folder visibility, and stable pagination. All 146 frontend workflow tests, TypeScript, native build, and final production export pass. The broader native run passed 251 tests with 11 ignored and one failure in unchanged `audio::device_detection::tests::test_calculate_buffer_timeout_bluetooth`: 159.999996 ms versus an exact 160 ms assertion. This is not a fully green native suite. Evidence is in `/private/tmp/meetnola-trash-{native-final,regressions,workflows,types-final,static-final}.log`.

Activated and strictly signature-verified dev build: `20260909-note-trash`; prior executable: `/private/tmp/meetnola-before-note-trash`. Native validation moved only **Meetnola standalone note review** to Trash, verified exclusion and persistence after restart, cancelled permanent deletion, restored it into an already-open search, and checked its original text, current/previous enhancements, folder membership, title, and persistence after a second restart. The note is restored and the app is on Home. No native note was permanently deleted and no audio was captured. Synthetic browser checks covered read/restore/delete failures and retries, cancellation with zero delete calls, disabled mutation controls, empty Trash, final-page fallback, and focus return. The initial confirmation focus escape was fixed. Trash and confirmation had zero internal horizontal overflow at actual 320×560, 390×740, and 960×800; the 320px fixture page reported 2px overflow with no element extending beyond the viewport, while the other page widths had none. The temporary preview is closed/stopped and viewport override reset. Model settings and standalone tester are unchanged; all work remains uncommitted on main with nothing pushed. Factual summary fidelity remains unresolved.

Find in note: the current Granola note surface exposes Find, while Meetnola's old summary Find callback was an unimplemented placeholder. The saved-note editor now offers **Meeting actions → Find in note** and `⌘F`/`Ctrl+F`, with literal case-insensitive matching, counts, previous/next wraparound, and keyboard navigation. It searches the visible written/enhanced editor and follows view changes without including the hidden document or assistant. CSS custom highlights paint ranges without inserting editor marks. Matches refresh after edits without scrolling away from the typing position; explicit navigation reveals the active range, including one inside a long paragraph. Results cap at 1,000 with a visible `+`. Source dialogs retain their shortcuts. The control adds no persistent toolbar row when closed. The existing editor, save behavior, model, and native binary remain in place.

Find validation: 146 frontend workflow tests and TypeScript pass. Native WebKit checks covered menu focus, `⌘F`, navigation, highlighting in both note views, source-dialog focus, and Escape; a duplicate React key and Radix close-focus race were fixed during testing. Native synthetic note text remained unchanged. The browser fixture used the production finder and BlockNote editor: phrases spanning bold/plain text, literal punctuation, hidden/outside scope, no matches, view switching, live edits, and focus return passed. Search alone caused zero editor writes. For a 123-match note, Shift+Enter wrapped to the final match while the bar stayed at y=0; edits near the end retained scroll position. A 1,100-match paragraph capped at **1000 of 1000+** with the active range visible. Actual 320×740, 390×568, and 960×800 viewports had zero horizontal overflow; the 320px close button ended at x=308. Native dev remains on `20260909-summary-storage` with frontend hot reload. The temporary browser fixture is closed/stopped and viewport override reset. All changes remain uncommitted on main, with nothing pushed. The final isolated production export and diff checks pass.

Enhancement storage recovery: a failed final write now uses the existing failure/backup restoration path instead of leaving the job pending. Generated titles are suggested only after the result saves. Edit saves reject missing results and active jobs, and completion writes reject missing process rows. The fault-injection test uses an in-memory database with real migrations and an AFTER UPDATE abort trigger; it verifies rollback of the current result and previous-version history, restoration of edited checklist data, unchanged title, and successful retry. A complete storage outage can still prevent recording the failure state.

Current activation and validation: dev build `20260909-summary-storage` is running and strictly signature-verified; its predecessor is `/private/tmp/meetnola-before-summary-storage`. All 113 native summary regressions pass (seven live tests ignored), including two new storage tests and the previous-version tests. Five synthetic HTTP tests initially failed because the sandbox blocked localhost listeners; the same suite passed outside the sandbox. Native testing on **Meetnola standalone note review** completed enhancement, saved a temporary edit, verified it after Home/reopen, removed the test character, and verified cleanup after full quit/relaunch. The custom title remained unchanged and the preceding enhancement was available in preview. This regeneration advanced the one-version history, replacing the older checklist fixture. No fault was injected into the app database. Native build and diff checks pass; frontend code is unchanged in this pass. No audio or model settings changed. Changes remain uncommitted on main; nothing was pushed.

Previous enhancement: the saved-note editor now offers a read-only preview and reversible restore from **Meeting actions**. A SQLite trigger preserves one prior result on successful enhancement; failed/cancelled jobs retain their incumbent backup behavior. Restore swaps versions transactionally, checks the current revision and selected version, refuses active jobs, and handles retries without swapping twice. Pending enhanced edits drain before preview reads. Original notes, transcripts, and titles stay outside this operation. This is one prior version, not full edit history or recovery of already-discarded results. See `docs/afterword-maintenance.md` for the user flow.

Previous-enhancement validation (historical activation): dev build `20260909-previous-enhancement` was activated and strictly signature-verified; its predecessor is `/private/tmp/meetnola-before-previous-enhancement`. Two native recovery tests, 144 frontend workflow tests, TypeScript, final isolated production static export, native build, and diff checks pass. Native testing on **Meetnola standalone note review** covered the initial empty state, an edited checklist before regeneration, read-only structured preview, restore and reverse restore, preservation of original written text/custom title, and persistence of both versions through complete quit/reopen. At that validation point, the synthetic document was the newer enhancement and the edited older checklist was recoverable. Browser checks with the actual dialog/hook and synthetic IPC covered read failure/retry, restore failure/retry, focus return, 390×568 and 960×800 viewports with zero horizontal overflow, and long content scrolling with visible actions. The fixture is stopped/closed, viewport override cleared, and native window size restored. No audio or model settings changed; standalone tester is untouched. All changes remain uncommitted on main, with nothing pushed. Factual summary accuracy and overall superiority to Granola remain unproven.

Summary completion safeguard: HTTP-backed enhancement now rejects explicit truncation, filtering, and other incomplete termination signals. Empty or reasoning-only output also fails at extraction, combination, final report, and text transformation boundaries. A truncated transcript part stops generation before final synthesis. Older compatible servers without termination metadata remain supported; absent metadata cannot establish completeness. The existing failure path restores the previous summary. No prompts or model settings changed.

Completion-safeguard validation: 108 native summary tests passed (six opt-in tests ignored at that run), followed by a new opt-in test against selected local `gemma4:e4b-mlx` that correctly rejected a deliberately one-token response. The native build and strict signature verification passed. That pass activated `20260909-summary-completion`; its predecessor is `/private/tmp/meetnola-before-summary-completion`. Native verification completed ordinary enhancement of **Meetnola standalone note review**, retained its original written text and custom title, and showed the saved enhancement after Home and reopen. Previous-summary restoration on an induced native failure was not separately exercised. The frontend was unchanged in that pass; its prior validation below still applies. All changes remain uncommitted on main, and nothing was pushed.

Latest fidelity experiment: the unchanged production pipeline passed 11/16 synthetic cases in 68.74 seconds. Revised default-template section instructions also passed 11/16 in 72.69 seconds and were rejected: they still dropped written requirements, assigned a disputed task, and misplaced pending spend. A test-only factual source-extraction pass followed by the original report pipeline passed 10/16 in 90.34 seconds. Its ledger retained the long-case editing requirement, but report synthesis dropped it and listed both disputed briefing owners as tasks; in a short case extraction also lost an owner. The extra pass was removed. These single runs do not establish general performance or causal differences. Reports: `/private/tmp/meetnola-fidelity-{baseline,template,source-pass}-sep09.json`, including the intermediate ledger for the extra-pass experiment. Manual review also found an incorrect combined rejection claim even where the budget detail said not approved; the phrase fixture and a regression now catch that observed contradiction. All 114 deterministic summary tests pass (seven opt-in tests ignored). Production template, prompts, model, and active native build are unchanged. This evidence rules out shipping either attempted fix; factual fidelity remains open, including loss during both extraction and final synthesis.

Factual quality remains unresolved: the fresh `varied-release-review` evaluation (2,063 transcript words plus written notes, runtime context, selected Gemma) took 18.15 seconds and failed five checks. It omitted Alex, Tuesday, and the editable-notes requirement, assigned the disputed customer briefing to Lee despite unresolved written-note evidence, and omitted the internal restriction from Key Decisions. Evidence: `/private/tmp/meetnola-summary-sep09-baseline.json`. Completion safeguards do not fix these errors or establish superiority to Granola. Next address conflict preservation and requirement coverage against this fixture without trading away the existing short-case checks.

Conflict controls: two new synthetic cases distinguish an unresolved written-note/transcript assignment from an explicit correction; both also require preserving a typed-only requirement and an unrelated confirmed task. The evaluator accepts the valid paraphrase "withdrew" and has regression coverage for both controls. All 109 non-live native summary tests pass (seven opt-in tests ignored). The unchanged Gemma prompt passed 12 of 16 bounded live case checks; failures include completed work placed under decisions, the long source conflict, dropped typed requirements in both controls, and the unrelated task omitted from the unresolved case's action list. A candidate conflict rule passed 11 of 16 and was removed: it improved named conflict coverage in the long case but still omitted the requirement and misplaced an approved decision in a short case. The unrelated task omission also occurred at baseline, so it is not established as a candidate regression. Installed Qwen 9B MXFP8 took 18.85 seconds on the long case, chose Alex despite the unresolved conflict, and omitted the editing requirement and intact-source fact; no model switch was justified. These are single synthetic samples, not general model rankings. Reports: `/private/tmp/meetnola-conflict-controls-baseline-sep09.json`, `/private/tmp/meetnola-conflict-rule-sep09.json`, and `/private/tmp/meetnola-qwen9b-long-sep09.json` (the Qwen report predates the corrected "withdrew" check). Production prompt, selected model, and active native build remain unchanged; only evaluation coverage and this handoff changed in this pass. Next test a source-preservation approach against these controls before adding more prompt rules or an automatic correction pass.

Source-wrapper evaluation: a JSON-labeled written-source candidate still omitted the editing requirement and conflicting values; it was rejected without changing the app. The evaluator no longer duplicates the frontend wrapper: `frontend/scripts/eval-summary-quality.cjs` imports `buildEnhanceNotesPrompt`, supplies synthetic contexts to the native harness, and records the exact context in each report. Its verified unresolved-case run preserved the editing requirement and unrelated task but omitted Riley, demonstrating variable output even with the unchanged prompt. All 109 non-live summary tests pass (seven opt-in tests ignored); diff checks pass. Reports are `/private/tmp/meetnola-written-source-wrapper-sep09.json` and `/private/tmp/meetnola-production-source-wrapper-sep09.json`. No production UI, prompt, model selection, or active build changed. Use the new script for future comparisons; do not infer causation from a single candidate/baseline response.

Standalone notes: **Save note** now saves written text without audio and opens the existing saved-note editor. SQLite creates the meeting, original notes, and optional folder membership in one transaction. A locally persisted submission UUID survives failed responses/restarts; **Retry save** reuses it, without duplicating a note or overwriting later library edits. Draft edits and recording stay disabled until that pending submission completes. The recording folder helper was also corrected to use `meetnolaInvoke`; its earlier bare-command call was missed by mocks. A regression now exercises the real IPC wrapper and verifies the plugin command name.

Current validation: 140 frontend workflow tests, TypeScript, three native standalone-note tests, production static export, native build, and diff checks pass. Native build `20260909-standalone-notes` is activated and strictly signature-verified; its predecessor is `/private/tmp/meetnola-before-standalone-notes`. Native testing deliberately hit the older backend's missing command, retained the locked draft, quit/reopened with the new build, retried successfully, verified zero transcript segments, folder count two, written-note search, scratchpad cleanup, and persistence after another full restart. Retained synthetic note: **Meetnola standalone note review**, `meeting-note-4b34616b-0a71-4181-99ca-24ae40f671dc`, in **Meetnola product reviews**. No audio was captured, no models or standalone tester changed, and nothing was committed or pushed. Narrow-screen validation remains unverified. Next prioritize factual summary quality against representative meeting sources; overall superiority to Granola remains unproven.

Local folders: create and rename folders from Home/sidebar; Organize note on Home and saved documents adds or removes individual memberships, including creating and assigning a folder atomically. Notes may belong to multiple folders. Folder views, search pagination, source links, clearing search, and return navigation preserve the selected folder. Memberships are separate from recording paths and source content. Behavior and current limits are documented in `docs/afterword-maintenance.md`.

Folder validation: 130 workflow tests, TypeScript, 17 Meetnola native tests (one live-model test ignored), four transcript persistence/migration tests, native build, production static export, and diff checks pass. The folder pass activated and strictly signature-verified native build `20260909-note-folders`; the prior executable is backed up at `/private/tmp/meetnola-before-note-folders`. Native verification created a folder with the existing synthetic saved-note review, removed/re-added membership, renamed it to Meetnola product reviews, checked the sidebar count, folder search (one match versus three globally), source navigation, Back to search/folder, document-menu membership, and persistence after complete quit/reopen. That pass retained folder `d019924d-9b7d-481f-84fe-7d5be0aae638` with one synthetic note; the standalone pass above adds a second. No source content, recordings, models, or standalone tester changed.

A controlled browser fixture using production folder controls verified read failure/retry, independent memberships, create-and-add, duplicate-name errors, and desktop/390px phone layouts, including 568px height and zero horizontal overflow. Checks fixed focus loss after mutations and dialog close, checkbox shrinking beside long names, and constrained dialog height. Native focus restoration was also verified. The fixture is closed/stopped and viewport restored; normal dev remains running. Work is uncommitted on main and nothing pushed. Nesting, deletion, and sharing remain absent; general superiority to Granola is still unproven.

Contextual creation now carries the selected folder through draft storage, recording startup, editor/tray stop, and interrupted-session recovery. Existing drafts retain their original folder; the editor shows its folder and returns there. Folder writes precede retry-data cleanup and reuse the saved recording's stable ID. The current frontend passes 137 workflow tests, TypeScript, and production static export. Seven new tests cover draft retention/encoding, reload, startup navigation/failure, editor/tray failed assignments, and recovery retry. Native dev verified an empty draft inheriting Meetnola product reviews, synthetic edits surviving full quit/reopen from global Home, and Back to folder. Only the temporary synthetic draft text was cleared afterward. That pass used native executable `20260909-note-folders`, with updated frontend served on 3118. No audio was captured in this pass; stop/recovery behavior was tested with synthetic fixtures. A browser-only preview reached native setup instead of the editor, so narrow-screen validation of this addition remains unverified. The standalone save action was added in the subsequent pass above.

Source navigation: search results now retain the indexed source identity and open a focused current excerpt in the existing Meeting sources panel. Transcript matches retain recording time; written-note matches open the Written notes tab. Show full transcript/Show all written notes returns to the incumbent source reader. The bounded read verifies meeting, source ID, source kind, and the query against the current index, without loading the complete transcript to locate one hit. Failed reads offer retry; edited/deleted/nonmatching sources are reported separately. Full-source expansion restores focus to the source controls, and closing a search-opened panel returns focus to Back to search.

Validation for source navigation: 128 frontend workflow tests, TypeScript, seven native search tests (one live-model test ignored), native build, production static export, and diff checks pass. Tests cover source ownership, edits/deletion, stale reads across meeting/source/query changes, failure/retry, missing passages, HTML escaping, and recording-relative time. The source-navigation pass activated and strictly signature-verified native build `20260909-search-sources`; the previous executable is backed up at `/private/tmp/meetnola-before-search-sources`. Native verification opened the exact 0:45 synthetic transcript passage and original written-note match, expanded both sources, switched tabs, and confirmed Return after closing activates Back to search with the query intact. A synthetic fixture using the production reader verified loading, retry, removal, and 390px/960px content surfaces with zero horizontal overflow for long unbroken text. It does not establish full mobile app layout or a native full-reload check. The fixture is closed/stopped and normal dev remains running. Notes, transcript, models, and standalone tester were unchanged. Work remains uncommitted on main; nothing pushed.

Latest library pass: Home now searches meeting titles, original written notes, and transcripts through the existing local FTS index. The previous title-only search returned zero results for a known transcript word. Results show a source excerpt, Written notes/Transcript/Title match label, recording time where available, and date. A separate discovery query returns one result per meeting in 30-result pages; it does not reuse chat's sixteen-excerpt limit. Title-only notes remain discoverable, title substring matches rank first, and body terms use the existing stemming/token matching (any supplied term). Generated summaries are not searched. Existing Recent/All notes, draft, recording, and ordinary timeline actions remain in place; search results open the meeting for further actions. That comparison identified folder organization as a gap; the current folder pass below adds local grouping.

Validation: 126 frontend workflow tests, TypeScript, five native library-search tests, and the final production static export pass. Tests cover written/transcript/title-only matches, one result per meeting, 65-meeting pagination, source edits/deletion, invalid search input, stale reads, overlapping load-more requests, and failed-page retry. A controlled browser fixture with production hook/results UI verified pagination, result destination, no matches, retry, date-only imports, and 390px/960px surfaces with zero horizontal overflow and 72px (three-line) excerpts. Visual review caught and fixed a conflicting display class that disabled clamping and date-only values shifting to the previous day. This fixture does not establish full mobile native layout. It has been closed/stopped; normal dev remains running.

The initial library pass activated and strictly signature-verified development build `20260909-library-search`. The new read-only command is in both plugin dispatch and the existing build-time command allowlist; native testing caught the initially missing allowlist entry before the final rebuild. Comparing unsigned temporary executable copies found a single differing byte in the `__LINKEDIT` virtual-size field after signing; all other bytes and lengths match. The standard template matches source. Native search found the synthetic saved-note review by transcript text (`custom`) and original written-note text (`different`), showed the 0:45 transcript excerpt, and opened the correct saved document. The subsequent navigation pass adds Back to search: opening a search result carries the current query in the meeting URL, including edits made without pressing Enter. Returning rebuilds the Home search route through encoded parameters and preserves the existing save-before-leaving checks. Normal timeline openings retain Home; failed/missing meeting views use the same return destination. Original notes, transcripts, model settings, and standalone tester were unchanged; no live database was inspected from the host. This pass remains uncommitted with the earlier navigation fix and experimental source-audit harness. General superiority to Granola remains unproven.

Return-to-search validation: all 126 workflow tests, TypeScript, production static export, and diff checks pass. Native checks confirmed the custom query round trip, a changed custom& query encoded intact, clearing search, ordinary Home navigation, and the button layout. Query state comes from the URL on mount, but a full native reload was not independently verified: the reload shortcut produced no observable transition, and a plain-browser direct load encounters onboarding without the native bridge. Search pages and scroll position are not retained. That pass found standalone punctuation rejecting otherwise valid queries; the following native correction resolves it. These frontend changes remain uncommitted on main and are active on the existing dev server. No native binary or saved meeting content changed in this pass.

Punctuation correction: discovery now ignores standalone symbol tokens for content matching while retaining escaped literal title matching, including symbol-only titles. Word/length bounds and literal treatment of FTS operators remain in place; multiword searches still match any supplied word, without an exact-phrase syntax. Six native search tests pass (one live-model test intentionally ignored), covering punctuation, quoted words, symbol-only title matches, wildcard safety, paging, and source updates. The punctuation pass activated and strictly signature-verified native build `20260909-search-punctuation`. The exact previously failing query different & custom returns six matching notes; & alone shows the normal zero-results state. The previous development executable is backed up at `/private/tmp/meetnola-before-search-punctuation`. No frontend changes were needed in this correction; prior frontend checks remain applicable. The standalone tester, notes, transcripts, and model settings were unchanged. Work remains uncommitted on main, with the normal dev server running and nothing pushed.

September 9 checkpoint: `45363bf` commits the source-review controls, selected-statement checks and synthetic evaluations, note-loading readiness/retry safeguards, and enhancement source preparation described below. Earlier uncommitted statements describe their original validation passes.

September 9 continuation checkpoint: `6661e7e` commits the duplicate-admission, cancellation-confirmation, and reopened-progress changes below together with longer summary evaluation coverage. Validation is 117 frontend workflow tests, TypeScript, production static export, and 104 native summary tests (five opt-in evaluations ignored). Earlier uncommitted statements describe their original passes; nothing is pushed.

Post-checkpoint fix: cancellation/failure restoration previously checked request ownership only before starting its asynchronous read. A delayed result could replace the next meeting's displayed document or status. The hook now rechecks request and meeting identity after restoration succeeds or fails; departed callbacks also cannot release the current generation lock. Eight new cases failed against the checkpoint and pass with the fix, covering successful/failed restore reads after navigation/unmount. All 125 workflow tests and direct installed TypeScript checks pass. A browser fixture using the production hooks and enhancement controls held A's restore, opened B, then released A; B retained its own document and processing/Stop state. This was synthetic IPC, not induced native storage latency. The fixture was closed/stopped; the normal development frontend remains running. This narrow follow-up and its tests remain uncommitted on main; no new native or production build was needed for this hook guard. Summary omissions and unresolved source conflicts remain the next quality issue.

Source-review experiment: a general second-pass audit missed the known written-note omissions and complained about already-covered material. Adding a notes-only instruction while retaining the transcript still missed them. Supplying only written notes and the draft caught the missing Alex/Tuesday values and editable-notes requirement twice (4.82 and 2.82 seconds for the long draft); a corrected draft no longer triggered those findings. However, controls exposed irrelevant metadata findings and quotes placed in the wrong field. No audit or automatic correction was added to production, and no model setting or native build changed. The retained opt-in harness checks exact source provenance and requires human semantic review; valid quotes alone do not validate a conclusion. All 105 deterministic native summary tests pass, with six live evaluations excluded. These evaluation additions remain uncommitted alongside the navigation fix. Next use this evidence to assess a reviewable written-note coverage aid, not a general accuracy badge or automatic reconciliation of conflicting sources.

Reproduce with `CARGO_TARGET_DIR=target/afterword AFTERWORD_REVIEW_SCOPE=notes cargo test --manifest-path frontend/src-tauri/Cargo.toml --features afterword --lib live_summary_source_review -- --ignored --nocapture`. Six tracked drafts in `frontend/tests/fixtures/summary-source-review.json` include faithful, deliberately incomplete, superseded-assignment, and corrected-long controls; expectations are not sent to the model. `AFTERWORD_REVIEW_INPUT` can select another synthetic summary report; omitted scope audits all sources. `AFTERWORD_EVAL_MODEL` and `AFTERWORD_EVAL_REPORT` select the local model and report path. Semantic review is required even on exit zero. Evidence is in `/private/tmp/meetnola-source-audit-{long,notes,notes-controls,isolated,repeat}.json`; the isolated/repeat runs used the retained transcript-free coverage prompt. Earlier runs used rejected prompt/input variants. No real meeting data was used.

Longer summary evaluation: added a varied 2,063-word synthetic release review with reassigned work, withdrawn approval, an unresolved written-note/transcript disagreement, and a typed-only editing requirement. The runner now supports `AFTERWORD_EVAL_CASE=<id>` and `AFTERWORD_EVAL_CONTEXT=runtime` (actual model metadata sizing), or a positive numeric context; omitted context retains 4,000 tokens. Reports include input size, threshold, and chunk count. The current Gemma model used one chunk at threshold 130,772 and took 12.95 seconds, retaining three assignments but omitting named conflicting values and the editable-notes requirement. Extra preservation instructions remained unreliable in two runs; moving written notes before the transcript introduced a false firm assignment. Both experiments were rejected and `processor.rs` restored exactly. Reports are `/private/tmp/meetnola-varied-long-{baseline,candidate,instruction-repeat,context-first}.json`. Production prompt, native build, and model selection are unchanged. These synthetic checks do not establish real long-recording quality or superiority to Granola; preserving typed requirements and unresolved source conflicts remains open.

Latest continuation: reopening a pending/processing enhancement now restores progress and Stop using the loaded native status, without starting another model request. Generation and reconnection share result handling. Saved and post-recording pages pass status with the meeting identity; late reads cannot restore the wrong meeting. Stop remains available before written notes load. The existing polling logic now lives in `useSummaryPolling`: stable callbacks and per-meeting ownership prevent rerenders or another meeting from clearing timers; slow reads do not overlap, and replaced/unmounted observers ignore late responses. Navigation also invalidates unfinished source preparation before it can launch an old request or unlock a newer one.

Verification: 117 workflow tests, TypeScript, production static export, and diff checks passed. A controlled browser fixture used the actual generation/polling hooks and enhancement controls with synthetic IPC: reopening kept the model-start count unchanged, Stop worked without note readiness, confirmed cancellation restored the prior document, and completion reached the reopened editor. The native app restored Stop after Home/reopen during enhancement; the short job completed before Stop took effect, and the new summary appeared with completion feedback. No false cancellation was shown. Original notes/transcript, model settings, and native build `20260909-0930-job-admission` were unchanged. The fixture was closed/stopped; dev remains running. These changes remain uncommitted on main. Next broaden native workflow and summary-quality checks with realistic longer meeting input; general superiority to Granola is still unproven.

Post-commit continuation: rapid generation/regeneration requests previously entered transcript reads concurrently. The shared hook now guards both entry points before preparation, retains the guard through active polling, and permits another attempt after preparation/startup failure or confirmed completion/cancellation/error. Stale polling callbacks cannot unlock a newer request. The regression failed on the committed baseline and passes with the fix. Native string errors now retain their explanation instead of displaying Unknown error. All 113 workflow tests, TypeScript, production static export, and diff checks passed.

Native admission now reserves each meeting before resetting process or transcript data. An owned job keeps its cancellation token through final database writes, then releases it on completion, early failure, or task abort. Duplicate callers in the same native process are rejected before writes and cannot replace the first token. Stop only requests cancellation; the worker publishes the terminal state after restoring the backup. Three new tests cover twelve simultaneous attempts, independent meetings, token identity, cancelled-but-running rejection, startup failure, and abort cleanup. All 103 native summary regressions passed (five opt-in model evaluations excluded); four synthetic local HTTP tests required running outside the sandbox to bind localhost.

Activation: development build `20260909-0930-job-admission` is running and strictly signature-verified. Build/app executable code matched after removing signing metadata from temporary copies; the bundled standard template matches source. The previous executable is retained at `/private/tmp/meetnola-before-0930-job-admission`. Native Stop on the synthetic saved-note review preserved its previous summary; a subsequent enhancement completed and survived Home/reopen with its custom title. The short generation finished before a second native caller could be exercised; concurrent rejection is covered by native tests, not a forced UI overlap. Original notes, transcript, model settings, and the standalone tester were unchanged. All post-checkpoint changes remain uncommitted on main; dev remains running. The subsequent reconnection pass is recorded above. General summary accuracy and superiority to Granola remain unproven.

Enhancement source fix: the shared summary hook now includes original written notes for both generation and regeneration when a transcript exists. Notes-only meetings carry the notes once in the main input instead of duplicating them as additional context. Both pages use this shared preparation. TypeScript, 112 workflow tests, production static export, and diff checks passed. Tests cover mixed, notes-only, transcript-only, and edited-between-generations inputs. Native **Enhance again** on the synthetic saved-note review included details unique to the written notes and preserved the transcript requirement without inventing an assignment; Home/reopen retained the result. The legacy regeneration entry point was verified by request-level tests. Model settings and the native binary remain unchanged; broad model accuracy remains unproven.

Latest readiness fix: a failed written-note load now exposes Retry in the saved editor, source sheet, meeting chat, and post-recording view. Readiness belongs to the current meeting; late reads cannot supply the next meeting's state. Duplicate retries are suppressed, and retry cannot replace successfully loaded or locally edited notes. Chat, recipes, selected-claim checks, and enhancement wait for notes to load; a successful empty-note read still permits transcript-only work. The source loader independently rejects unavailable notes before fetching transcripts. Automatic enhancement now waits for the original notes and includes their existing enhancement prompt.

Verification: TypeScript, 109 workflow tests, production static export, and diff checks passed. Regressions cover failed/pending reads, duplicate retry, preserved edits, navigation, loaded-empty notes, and stopping chat/enhancement before requests. A fault-injected browser fixture used the real note-loading/chat hooks and source sheet with mocked storage/model calls: failure kept Send disabled and preserved the draft with zero calls; retry exposed the recovered note and the next request included it. The 390×844 recovery view had no horizontal overflow. Native Gemma answered a synthetic written-note question and opened the correct saved S1 excerpt. No native database fault or live recording was induced; the post-recording controls were checked in source/build, not through a new recording. The preview was stopped. All current changes remain uncommitted on `main`; native dev and model selection are unchanged.

Latest source-review UI: **Review sources** in saved-meeting chat opens the existing transcript sheet with **Written notes** and **Transcript** tabs. It exposes current originals independently of the model's citation choices and labels them separately from saved answer excerpts. The original Transcript shortcut still opens transcript search directly. Search survives tab switches; text with long unbroken words wraps inside the panel. No source snapshots or model settings were changed.

Behavior decisions: improve access to uncited original notes; preserve saved citation excerpts and cross-meeting chat scope; preserve transcript search, copy, refresh, and pagination; improve modal reliability with immediate opening/dismissal and focus returning to the actual trigger. Native WebKit left the sheet invisible while it held keyboard focus; removing the shared sheet animations fixed the observed failure. Source-tab selection also updates immediately. This shared primitive change also applies to the post-recording quick-note transcript sheet, which was not exercised during a live recording in this pass.

Verification: native checks on the synthetic saved-note review covered both source tabs, arrow-key navigation, Escape/trigger focus, direct Transcript/search focus, retained search, and saved cross-meeting citations/navigation. An isolated browser fixture used the production components and copied current sheet markup with synthetic data and the app font. At 390×844 and 960×800 the source sheet had zero horizontal overflow, including long unbroken text; 320×740 chat controls also fit. Search found segment 121 beyond the initial 100, pagination completed, and closing preserved the unsent draft. TypeScript, 105 workflow tests, production static export, and diff checks passed. The temporary preview was stopped; native dev remains running. These changes remain uncommitted on `main`. Model judgments under conflicting sources remain an unresolved accuracy issue.

Current continuation: enhanced summaries now offer **Check selected text**, also available with Command/Ctrl+Shift+Enter. It sends the selected statement to the existing saved-meeting assistant using only original written notes and transcript as evidence. The summary is not evidence, the unsent chat draft is preserved, and checks use the existing model, cancellation, citation snapshots, and conversation persistence. Selections outside the summary, empty selections, and selections over 1,200 characters cannot be submitted. These frontend changes and this record remain uncommitted on `main` after checkpoint `df696f1`.

Validation: TypeScript, all 104 workflow tests, and the twelve-page production static export passed. Native hot-reload verification on build `20260909-0809-summary-requirements` used only **Meetnola saved-note review**. Button and keyboard checks supported the real title-preservation requirement; a temporary invented Morgan/Friday assignment was flagged as unsupported. The temporary sentence was removed and the restored summary verified after Home/reopen. All three new checks persisted, and the restored citation opened the exact original transcript excerpt at 0:45. This is an on-demand model-assisted check, not an automatic factual guarantee or a comparative accuracy benchmark. The development frontend remains running; no native binary, model preference, or standalone tester was changed.

Harder claim evaluation: added eight synthetic cases covering changed owners, unconfirmed offers, compound claims, conflicting notes/transcript, withdrawn approval, negative wording, unresolved ownership, and instructions inside a selected quote. Both prompt experiments were rejected: one mislabeled prior approval as a proposal; the verdict-based version called an unconfirmed commitment partly supported. The original prompt is retained. Gemma caught explicit reassignments/withdrawals but still resolved conflicting written notes and transcript too confidently, omitting the notes conflict entirely in its repeat. Qwen 4B/9B did not resolve that weakness; 9B additionally named the wrong people to contact in the ownership-disagreement case. No model switch was made.

The repeat's seven subsequent Gemma calls completed in 0.15–0.41 seconds, versus 0.81–3.04 seconds for Qwen 4B and 1.53–3.98 seconds for Qwen 9B. First calls took 1.50, 3.83, and 7.55 seconds respectively. These sequential, short synthetic runs have different answer lengths and warm/cache effects; they do not establish representative long-meeting performance. Full responses and timing are in `/private/tmp/meetnola-claim-{baseline,candidate,verdicts,gemma-repeat,qwen4b,qwen9b}.json`. The evaluation additions, source checker, and handoff remain uncommitted on `main`. Refreshed TypeScript, 104 workflow tests, and 100 native summary regressions passed (five opt-in live tests excluded). Accuracy under unresolved source conflict remains open.

Commit checkpoint (September 9): the accumulated work is committed on main: `46ccb28` upgrades Next.js/React, `fc4850f` adds persistent conversations/search and the note-workspace fixes, and `344e270` preserves summary requirements and expands quality checks. Earlier uncommitted-status statements below describe their original validation passes. The pre-commit refresh passed TypeScript, 102 workflow tests, three Markdown tests, and eight native Meetnola regressions (one live-model test ignored); native summary/build and visual evidence remain recorded below. Nothing was pushed.

Post-commit investigation: confirmed that section instructions reach the model. Tested a Key Decisions clarification that explicitly retained approvals mentioned in Summary and excluded completed work/current status. The committed baseline misplaced the completed audit; its other flagged result was a valid paraphrase of intact source data. The candidate passed all thirteen cases once, but its repeat again put the completed audit in Key Decisions. The candidate was rejected and the template restored exactly to `344e270`; no native app or bundled template was changed. The experimental `20260909-0822-decision-placement` build was not activated. Do not copy that build into the app; rebuild from current source before any future activation. Evidence: `/private/tmp/meetnola-decision-baseline.json`, `/private/tmp/meetnola-decision-candidate.json`, and `/private/tmp/meetnola-decision-repeat.json`. Repeated template wording alone has not established reliable decision classification. The next pass should evaluate source-linked verification of extracted decisions rather than treating another single green phrase-check run as proof of accuracy.

Latest accuracy pass (September 9): expanded the local-model fixtures from nine to thirteen with varied conversations covering two owners/two deadlines, completed work versus new follow-up, separate approved/pending expenses, and an interrupted handoff. Checks now bind task, owner, and deadline within one action item instead of accepting their presence anywhere in the action section. Regression tests reject swapped owners/dates and an approval mentioned only in the overview while Key Decisions says None noted. Lexical checks accept faithful paraphrases; an observer's stated intention was not treated as an invented task merely because the observer owned neither main deliverable.

Baseline Gemma omitted the title-preservation requirement once and misplaced completed/pending status under Key Decisions. A template wording experiment did not consistently fix classification and was reverted. The retained production change adds one instruction to preserve stated requirements even when no follow-up work was assigned. That requirement survived both subsequent full-suite runs and the native saved-note check. Reports are `/private/tmp/meetnola-varied-baseline.json`, `/private/tmp/meetnola-varied-candidate.json`, `/private/tmp/meetnola-varied-requirements.json`, and `/private/tmp/meetnola-varied-final.json`. The final run passed the then-current thirteen-case checks, but manual review found the encryption approval absent from Key Decisions despite appearing in the overview. The new decision-placement regression was added afterward, so the final report is not a pass under today's stricter checks. Another run put the completed audit under Key Decisions. Classification remains variable and is the next quality issue to address; these short synthetic cases do not establish representative meeting accuracy or superiority to Granola.

Activation: native dev build `20260909-0809-summary-requirements` is installed, ad-hoc signed, and strictly verified. Binary and unchanged standard-template hashes match source/build artifacts. Native generation on the existing synthetic saved-note review preserved the stated requirement without inventing a task; Home/reopen retained the summary and custom title. Original notes, transcript, and model settings were unchanged. The standalone tester was not changed. All 100 deterministic summary regressions and diff checks pass; the new runtime build passed. No frontend code changed in this pass. All work remains uncommitted on main, with the existing Next.js dev server running and nothing pushed.

Previous interaction pass (September 9): fixed the stuck dropdowns by removing entrance/exit animation classes from the shared Radix dropdown content and subcontent. Radix otherwise retains its selection, keyboard, focus, portal, and outside-dismissal behavior. Native checks now pass for note-view selection, Home/Return keyboard selection, Escape, outside click, template selection, and Escape/Return reopening from the restored trigger focus. A narrower native window remained usable. This verifies the fix for the reproduced menus; other animated dialogs/sheets were not audited in this pass.

Enhancement feedback now uses inline progress instead of an independent timed start toast. Completion, cancellation, and errors share a per-meeting notification ID, and starting a new generation dismisses its earlier feedback. Stop waits for the startup acknowledgment, keeps polling until a terminal backend result, and reports cancellation failure without falsely resetting the job to idle. Five regressions cover cancellation, completion races, request failure, delayed startup, and startup failure. Native local-Gemma generation completed, and a subsequent immediate Stop showed confirmed cancellation with the previous summary intact. Original notes, transcript, title, model, and template selection were preserved; only the synthetic enhanced summary was regenerated. Sonner's existing hidden-document/hover timer behavior is unchanged; this pass fixes conflicting generation messages, not every notification timing behavior.

Validation: 102 workflow tests, three Markdown tests, TypeScript, the production static export, and diff checks passed. Changes are active through the existing Next.js development server and remain uncommitted on main. The standalone tester is unchanged. Next broaden summary evaluations beyond the repetitive synthetic fixture; general factual accuracy and superiority to Granola remain unproven.

Previous upgrade pass (September 9): upgraded the frontend from installed Next.js 14.2.35 to 16.3.4 and React 18 to 19.2.8. Retained Webpack explicitly for the existing BlockNote/ProseMirror aliases and Tauri static export. Migrated async route params, React ref types, the removed lint/export commands, and ESLint flat configuration. Removed unused Remirror dependencies that constrained React compatibility. The lockfile and five build workflows now use pnpm 9.15.9; workflows use Node 22, with Node 20.9 as the minimum. Next.js generated frontend AGENTS.md and CLAUDE.md guidance files.

Upgrade validation: frozen lockfile installation, TypeScript, all 97 workflow tests, three Markdown tests, production build with all 12 static pages, and diff checks passed. Native checks verified a clean relaunch, the saved synthetic note, editor input and autosave, restoration of the original synthetic summary after testing, saved chat citations, and the library conversation with its draft and Last 30 days scope intact. The first development load hit a chunk timeout; the asset returned HTTP 200, webview Reload recovered, and subsequent full relaunch loaded normally. The pre-existing native note-picker dismissal issue reproduced and required graceful Quit/reopen. Lint now runs but reports 273 errors and 161 warnings across the codebase under the upgraded rules; this is not a clean lint baseline. ESLint 9 remains because the React plugin does not yet declare ESLint 10 compatibility.

The upgraded frontend remains running on port 3118 (session 32111; log `/private/tmp/meetnola-next-dev.log`) against the existing native development build `20260909-0734-summary-fidelity`. No native binary or standalone tester was rebuilt for this frontend upgrade. All prior work and this upgrade remain uncommitted on `main`, with nothing pushed. Resume popup/notification lifecycle fixes and broader summary evaluation after this dependency pass.

Current pass (September 9, 07:39 PDT): improved summary fidelity and verified build `20260909-0734-summary-fidelity` in the native dev app. The pipeline now sends extracted transcript parts directly to final formatting when they fit after accounting for the template and typed-note overhead, avoiding an unnecessary intermediate model rewrite. Oversized extracted notes retain the existing combine path. Final instructions preserve later explicit owner/deadline corrections; the standard template places pending proposals and uncommitted offers in Discussion Highlights. Other templates retain their own section structure.

Evidence: the selected `gemma4:e4b-mlx` passed all nine existing short cases before and after the changes. The new 14,089-character synthetic fixture separates an early Noah/Monday assignment from its later cancellation and Liam/Tuesday replacement. It deliberately uses repeated review material and a 4,000-token threshold to exercise two transcript parts; it is not a representative one-hour recording or a benchmark of the app's dynamically selected context size. Baseline generation omitted the final assignment in one run (35.6 seconds); a traced rerun retained it, establishing variability rather than a deterministic failure at one stage. The final version passed two repeats at 26.7 and 28.1 seconds, retaining Liam/Tuesday, intact records, the internal pilot, and the pending budget outside Key Decisions. Section checks now catch budget placement, while manual review still found other scope statements presented as decisions in one repeat. These checks do not establish general factual accuracy or superiority to Granola. Reports are `/private/tmp/meetnola-accepted-short.json`, `/private/tmp/meetnola-accepted-long-1.json`, and `/private/tmp/meetnola-accepted-long-2.json`; the opt-in evaluations and deterministic regression are in `frontend/src-tauri/src/summary/quality_evals.rs`.

Activation corrected an earlier evidence gap: the development app's bundled `standard_meeting.json` was still the old table-and-timestamps template, despite prior source updates. There was no custom standard template override. Both the tested binary and standard template were copied into the existing dev bundle, matched against source by SHA-256, and the bundle was ad-hoc signed and strictly verified. Future manual activations must verify changed bundled resources as well as the binary. The standalone tester was not changed.

Native checks confirmed build `20260909-0734-summary-fidelity`, idle recording state, local Gemma enhancement of the existing synthetic saved-note review, no invented task, preserved custom title, and the regenerated summary after Home/reopen. The original written note and transcript were not edited. Visual checking found the document remained transparent while its entrance animation failed to complete; replacing the outer motion wrapper with a normal div made the note immediately visible and still visible after navigation. The final source passes 98 summary regressions, 97 frontend workflow tests, TypeScript, native build/signature checks, and diff checks. The frontend-only animation removal was hot reloaded after that native build. No new production frontend build was run for that wrapper removal.

Remaining UI observations: the native template popup resisted dismissal through Computer Use until graceful Quit/reopen, and generation/success notifications lingered together during this check. Do not infer either root cause from the automation behavior alone. Next inspect notification lifecycle and popup/focus behavior, then broaden evaluations beyond repetitive synthetic input. All current work remains uncommitted on `main`; nothing was pushed. The broader Granola goal remains active.

Previous conversation pass: cross-meeting conversations now persist in SQLite. **Conversations** reopens history, **New chat** starts a separate conversation, and **Archive / Restore** keeps old chats recoverable. Drafts and date ranges belong to their conversation and save through the existing quit-flush mechanism. Titles derive from the first question or unsent draft. The picker supports Recent/Archived views and pagination. Loading gates edits; failed saves and loads retain data and expose Retry. Saved-meeting chat retains its existing behavior through the shared persistence hook.

Source lifecycle: only cited excerpt text is stored, together with IDs for all consulted meetings. Deleting a source meeting removes affected generated answers and their copied excerpts from active and archived library conversations; user questions and unrelated answers remain. Late saves recheck source existence and cannot restore removed text. Archived conversations reject late message/draft updates. These controls operate in the existing SQLite database; enhancement still uses the selected provider, with no model-setting change.

Verification: 96 frontend workflow tests, three Markdown tests, TypeScript, nine native Meetnola tests, and final frontend/native builds passed. Native coverage includes an actual temporary SQLite file closed and reopened with the conversation, unsent draft, and date filter intact; source deletion, late writes, independent conversations, and archive protection also passed. Browser checks used the actual page/hooks/components with a synthetic persistence/model backend: separate conversations, navigation and reload recovery, preserved draft/date scope, restored citations, archive/read-only/restore, and the history picker. At 390×844 and 960×800 there was zero document horizontal overflow. Resizing a restored draft from desktop to narrow now changes textarea height from 36px to 76px, matching its scrollHeight; its bottom remained at 815px in the narrow viewport. This fixes clipping caused by sizing only on input changes. The simplified preview shell and synthetic backend do not establish native app activation or real multi-window behavior.

Previous cross-meeting pass: **Ask your notes** adds cross-meeting questions through Home and the sidebar. A dedicated, responsive chat page reuses the streaming assistant, Stop, citations, Copy, focus restoration, and confirmation before Clear. That first version retained conversations only during the session; the persistence pass above supersedes this limitation. Durable saved-meeting conversations remain unchanged. Errors offer Edit question, and the status announcement distinguishes failure from a completed answer.

Retrieval uses the existing SQLite database and selected enhancement client. The new [FTS5](https://www.sqlite.org/fts5.html) index covers titles, original written notes, and transcripts, with transactional maintenance for edits, moves, renames, and deletion. It does not index generated summaries or chat answers as evidence. Date filters offer All time and rolling 7/30/90-day ranges. Query words are quoted and bounded; short reference questions reuse the previous substantive topic. Retrieval ranks across matches, limits each meeting to four excerpts and the answer context to sixteen excerpts, and caps excerpt length. Each answer discloses search words, date range, and source coverage. Citations retain the exact excerpt, meeting title/date, recording time where available, and an Open meeting link. This is keyword retrieval, not exhaustive semantic search; relevant material outside the excerpts can be missed.

Verification: 91 frontend workflow tests, three Markdown tests, TypeScript, six native Meetnola tests, final isolated production frontend build, and native development build passed. A separately invoked test through the actual local `gemma4:e4b-mlx` client retrieved two synthetic indexed meetings and correctly answered that Mira owns the launch while the budget is proposed, not approved, citing both sources. This single short case does not establish general factual accuracy or large-library performance. SQLite tests cover backfill, date filtering, Unicode, edits/moves/rename/deletion, literal FTS operators, and diversity when one meeting has 200 matching segments. No live meeting database was opened from the host.

The isolated browser preview uses the real page/hooks/components with a simplified shell and simulated retrieval/streaming. At 390×844 and 960×800 it had zero document horizontal overflow; the composer bottom was 815px and 771px respectively. Verified navigation retention, source excerpt and meeting-link destination, no-match feedback, Edit question, date selection and per-answer coverage, Clear confirmation/Keep, and Jump to latest with focus in the conversation. Visual checks caught and fixed a date-only import shifting to the previous day. The preview is not evidence of the native IPC path or live app layout.

Native activation and verification completed September 9: build `20260909-0053-chat-history` is installed in `/private/tmp/meetnola Dev.app`, ad-hoc signed and strictly verified. The stopped development frontend was restarted on port 3118. An initial stale Next.js chunk timeout cleared through the native webview's Reload menu after verifying the asset was available; the subsequent full app restart loaded normally. Native checks used the existing synthetic **Meetnola saved-note review** and two synthetic library conversations. They verified a local streamed answer, the exact 0:45 transcript and original-note excerpts, Open meeting navigation, the existing per-meeting history, a follow-up using restored context, independent chats, retained multiline drafts/date ranges, and Archive/read-only/Restore. Command-Q fully exited the native process; after relaunch, both conversations, the first chat's Last 30 days scope and draft, and clickable citations were restored. No meeting notes, transcripts, or model preferences changed. The standalone tester remains unchanged.

Native checking exposed two issues missed by the simplified browser shell. Gemma emitted grouped citations such as `[S1, S2]`, which were plain text and therefore lost their excerpt snapshots on save. New answers now normalize known grouped IDs into individual links before persistence; the regression checks restored excerpts and leaves code, existing links, and unknown-only groups intact. The first synthetic answer generated before this fix still has plain markers and no saved excerpt; it was not rewritten using current source data. Subsequent grouped citations and their original excerpts survived full app restart. Also, the Ask page now uses the existing saved-note page's fixed viewport height: opening a long excerpt scrolls the conversation without pushing the composer below the native window. Wider and narrower native visual checks confirmed the draft and Send button remain visible. All 97 frontend workflow tests, TypeScript, and diff checks passed after these fixes; the nine native tests and final frontend/native builds above preceded these frontend-only follow-ups.

All four current batches and these native-verification fixes remain uncommitted on `main`, with nothing pushed. The development frontend remains running on port 3118. The broader Granola goal remains open: next prioritize harder factual-quality and long-meeting evaluations before adding more surface features. Calendar integration, shared spaces, folders, and multiple conversations within a single saved meeting remain separate gaps; no comparative accuracy or long-meeting performance claim has been established.

Previous persistence pass: saved-meeting conversations now persist in the existing SQLite database, including the original source excerpts actually cited by each answer. Loading gates new questions and ignores stale meeting results. Complete exchanges restore the last six turns of follow-up context; interrupted or failed answers do not. Writes use the existing ordered quit-flush queue, persist questions and completed answers rather than every streamed token, and retain visible partials on navigation or graceful Quit. Failed saves expose Retry. Clear requires confirmation; deleting a meeting cascades to its conversation, and late saves cannot recreate it. The per-meeting storage limit is 16 MB and fails visibly without truncating history. Live/quick-note chat remains session-scoped.

Previous native build: `20260908-2358-saved-chat` in `/private/tmp/meetnola Dev.app`, signed and verified, with frontend hot reload on port 3118. Native checks on **Meetnola saved-note review** verified an answer surviving Home/reopen, a follow-up using restored context, full Command-Q exit/relaunch, both restored exchanges, and the exact original 0:45 citation excerpt. The Clear dialog's Keep action preserved history. No meeting notes, transcripts, or model preferences changed; only this synthetic conversation was created. The broader Granola goal remains open.

Validation: 87 frontend workflow tests, TypeScript, a native persistence/isolation/deletion test, native build and signing, and isolated production frontend build. Coverage includes malformed-history refusal, load failure/retry, stale reads, storage minimization, incomplete-answer labeling, restored follow-ups, navigation/Quit checkpoints, and failed-save retry. Earlier design work's three Markdown tests also passed. Both the visual pass and this persistence pass remain uncommitted on `main`; nothing was pushed.

September 8 design pass: compared the running Granola Home, expanded navigation, saved note, and enhanced-note/template menu with Meetnola. Granola's document typography, compact note controls, right-aligned meeting times, and small bottom composer informed a targeted update on `main`. It uses the existing components and local model services; that visual pass changed no native code, model selection, or meeting content.

| Workflow | Decision and result |
| --- | --- |
| Home browsing and title search | Improve: serif page title, Recent/All notes controls, instant case-insensitive title filtering, clear/empty-result recovery, quieter search field, right-aligned times. Preserve date grouping, eight-item recent view, URL searches on Enter, draft, recovery, import, and row actions. |
| Saved-note reading and editing | Improve: compact My notes/Enhanced radio menu, serif document title, quiet regeneration control, less metadata and no header divider. Preserve both editors, autosave/retry, explicit Save in Meeting actions, model/template controls, and transcript access. Reserve scrollbar space so chat expansion does not shift document alignment. |
| Meeting assistant | Improve: slim idle composer, recipe menu and idle shortcut, growing multiline input, Enter/Shift+Enter and IME handling, answer copying, and follow-latest scrolling that pauses while reading older text. Preserve Stop, clear, complete source snapshots, exact citations, incomplete warnings, and six-exchange model history. Copy retains incomplete warnings; copy controls are hidden on the actively streaming answer. Reopening retains the current conversation and focuses the input. |

Verification: 81 frontend workflow tests, three Markdown tests, and TypeScript passed. Native checks on the synthetic saved-note review covered immediate title filtering, original/enhanced selection, multiline entry, a correct local answer with its 0:45 source excerpt, Copy feedback, Escape focus restoration, recipe-menu access, and conversation collapse/reopen. Wide and narrow native windows remained usable. An isolated browser fixture using the real Home and assistant components passed at 390×844 and 960×800 with zero document horizontal overflow; at 390px the input grew from 36px to 56px for two lines. With multiple long synthetic answers, a new update preserved scrollTop at 0 while reading earlier text; Jump to latest reached a zero bottom gap and focused the conversation. This fixture uses synthetic state and simulated answer updates, not the native backend or a model performance benchmark. Native screenshot/control interruption was resolved by opening the existing dev bundle through Finder. The running native build remains `20260908-2320-streaming-chat`, with these frontend changes active through hot reload.

The isolated production frontend build also passed on the final source; the existing Google Font fetch required network access. The synthetic browser preview and its development server were temporary verification tools, separate from the running native app.

Intentional differences and remaining work: retain the uniform white shell and local MLX/Parakeet providers. Calendar sync, shared spaces, folders, and multiple chat threads within a single saved meeting remain unimplemented. Cross-meeting questions and durable conversation history are implemented; native activation and verification are pending above. Per-meeting conversation persistence was added in the current pass above. Granola factual accuracy was not benchmarked. The live-recording flow was not exercised in the visual pass; its shared answer-copy change is covered by the component test and frontend checks.

Previous performance pass: performance and reliability fixes are committed as `80c7dc5` on `main`. Search is debounced, limited to 100 matching meetings with one excerpt each, and backed by a per-meeting transcript index. Search snippets handle Unicode safely; stale meeting responses cannot replace the current selection. Chat enforces its output limit and supports cancellation on Stop, clear, and navigation.

Ollama meeting chat now streams over a Tauri channel. First text appears immediately; later updates are batched every 50 ms. Stopped, failed, or length-limited answers are labeled incomplete and excluded from follow-up history. Known plain source markers become citation links; unknown IDs remain non-clickable. Streamed Gemma questions explicitly disable thinking, which otherwise consumed much of the 400-token budget. Summary generation keeps its previously tested temperature/reasoning profile. See [Ollama thinking controls](https://docs.ollama.com/capabilities/thinking).

Validation: 78 frontend workflow tests, three Markdown tests, TypeScript, 96 native summary/streaming tests, isolated frontend production build, native build, and signature verification passed. Three short synthetic Gemma streaming samples completed with source citations: first text 32–250 ms, completion 350–584 ms; later samples used a warm model/cache. This is not a long-meeting benchmark. Native checks on **Meetnola saved-note review** verified cancellation, a subsequent correct answer, its exact transcript citation, contextual follow-up, and the incomplete warning for an intentionally overlong request. The broader native suite still has an unrelated Bluetooth timeout precision assertion (159.999996 ms versus 160 ms).

Prior follow-up chat and Gemma sampling validation on `main`: Chat sends the last six successful exchanges separately from current meeting sources, removes old citation IDs from historical answers, and clears history with the conversation or meeting. Seventy frontend workflow tests, TypeScript, 92 native summary tests, an isolated production frontend build, and native build `20260908-2212-gemma-follow-ups` passed. The user approved restarting; that build is now installed, signed, and visibly active. Native UI checks on **Meetnola saved-note review** confirmed Gemma regeneration without the earlier invented implementation task, preservation of the custom title, a contextual follow-up with an exact transcript citation, and Escape focus restoration. A full quit/reopen preserved the Gemma selection, regenerated notes, and custom title. Parakeet Compact remains selected. The optional `live_meeting_follow_up_quality` test also passed against local Gemma, including rejection of an invented prior assignment.

Gemma recommendation: the user's requested small MLX candidate is `gemma4:e4b-mlx`, downloaded through Ollama. Google calls it E4B (8B total with embeddings, 4.5B effective); the installed artifact reports 8.1B, Safetensors, NVFP4, and Ollama shows 100% GPU execution. See the [Google model card](https://huggingface.co/google/gemma-4-E4B-it) and [official Ollama MLX tag](https://ollama.com/library/gemma4:e4b-mlx). Two default-temperature trials preserved all tested task assignments, but manual review of the repeat caught a validation condition attached to the wrong event. At temperature 0.2, all nine synthetic cases passed and manual review found the relevant facts intact: 2.58–4.58 seconds per case, median 3.23 seconds. The current Qwen baseline passed six of nine checks, median 1.08 seconds. These are short synthetic cases, not long-meeting performance claims. The new Gemma profile applies temperature 0.2 to Ollama Gemma 4 requests; other providers retain their settings. A narrow reassignment phrase check was expanded to accept "moved from Noah to Liam," with regression coverage still rejecting the old owner/deadline. Gemma is selected and its temperature 0.2 profile is active.

## Saved-note workspace and reliable edits

Active dev bundle: `/private/tmp/meetnola Dev.app`, build `20260909-0734-summary-fidelity`, with frontend hot reload on port 3118. The source-coverage fixes and updated standard template are installed and visually verified. Local Parakeet Compact and Ollama `gemma4:e4b-mlx` are selected; recording is stopped. The standalone tester remains unchanged.

Saved-meeting questions now retrieve the complete transcript through the existing meeting API instead of using only the loaded display page. Each answer retains its original notes/transcript source snapshot. Markdown citations open exact excerpts with recording timestamps where available; unknown IDs are non-clickable. Escape closes the excerpt and restores citation focus. Duplicate sends are suppressed; cleared, unmounted, or changed-meeting requests cannot append stale answers. Context retrieval failure stops the request before calling the model. Live/quick-note chat retains its existing context behavior; model context-window limits and factual accuracy remain separate concerns.

Citation validation: 68 frontend workflow tests, three Markdown tests, TypeScript, production frontend build, native dev build, signature verification, and diff checks passed. Native UI checks on the synthetic saved-note review confirmed a correct title-preservation answer, a transcript citation at 0:45, the exact original written-note excerpt, and keyboard dismissal/focus. A second question correctly declined to invent an implementation owner or deadline. This is bounded evidence, not general factual-quality acceptance; the earlier Qwen-generated summary contained an unsupported commitment, which the verified Gemma regeneration removed.

The saved-note toolbar is compact, with Copy in Meeting actions and accessible model/template controls. Generated sections use semantic H2 headings styled below the document title. Enhanced edits now save automatically through the existing native summary API on both saved-note screens. Writes are ordered, queued intermediate snapshots are coalesced, and Save remains available for retry. Status distinguishes unsaved, saving, failed, and saved. Pending writes continue after route unmount; regeneration awaits them and stops if saving fails. The editor is read-only during generation. Regenerated Markdown loads without falsely marking the document dirty. Switching note tabs preserves edits. Native window close hides the app and retains the running editor. The macOS Quit menu, Command-Q, and tray Quit now use a save handshake: flush delayed notes, drain title/summary writes, and exit only after success. The predefined macOS Quit item bypassed Tauri ExitRequested via Cocoa terminate; it is replaced with a custom item retaining the same label and shortcut. Failed writes remain retryable after their editor unmounts, and queues are shared per meeting so older failures cannot overwrite newer edits. A failed or timed-out flush keeps the app open. Recording, paused, processing, stopped-but-unsaved, and saving sessions block completion. Dock/system termination and force quit are not covered by this handshake.

AI title suggestions now apply atomically only to placeholder titles; explicit renames and existing titles survive enhancement. The frontend also checks the latest edited title before accepting a suggestion. Existing transcript display names remain consistent with explicit renames.

Validation: 62 frontend workflow tests, three Markdown tests, TypeScript, production frontend build, 88 native summary tests (one live-model test ignored), one native title-persistence test, two native quit-coordinator/session tests, native dev build, signature verification, and diff checks passed. Native UI checks used only the synthetic **Meetnola saved-note review**: checkbox save/reopen, retention across note tabs, Home save/reopen, regeneration without a false dirty warning, title preservation after regeneration/reopen, model/template controls and Escape focus, a half-screen layout check, immediate sidebar navigation after editing without Save, and window close/reopen after editing. Autosave regressions cover ordered/coalesced writes, unmount, failure/retry, and save-before-regenerate success/failure. Quit coverage includes the real delayed-note hook, stale failed writes after reopening, duplicate/stale quit requests, timeout/disposal, and native refusal. Native Command-Q was exercised while the synthetic original note visibly showed Saving; its final character survived full exit/reopen after the menu fix. An enhanced checkbox also survived quit/reopen. Recording refusal is covered by code/tests, without starting another recording. Earlier Qwen regeneration invented an implementation commitment from sparse source text; the Gemma comparison and current native verification are recorded above. General factual quality still requires broader evidence.

## Summary source coverage and quality evaluation

Run selected-summary checks from the repository root with `node frontend/scripts/eval-summary-claims.cjs`. This imports the actual frontend question builder and invokes the native streaming Q&A path with the eight cases in `frontend/tests/fixtures/summary-claims.json`. It uses local Ollama only; `AFTERWORD_EVAL_MODEL` defaults to `gemma4:e4b-mlx`, and `AFTERWORD_EVAL_REPORT` defaults to `/private/tmp/meetnola-claim-quality.json`. The command verifies request success, records complete answers, expected judgments, source text, and timing, and explicitly requires manual semantic review. Exit zero is not an accuracy pass. On macOS, run outside a restrictive sandbox if the HTTP client's system-configuration initialization fails.

Fixed two ways long meetings could produce incomplete summaries: sentence-boundary splitting could skip source text, and a failed transcript part was silently omitted. Chunking now advances from the actual boundary, including Unicode text; any failed part stops generation with a retry message. Existing cancellation and previous-summary recovery remain in place.

The standard template uses concise action checklists instead of a five-column table that demanded timestamps even for typed notes. It preserves discussion context and unresolved issues. Extraction prompts distinguish proposals, agreements, and commitments; local Qwen uses temperature 0.2 with direct answers. These are improvements to formatting and source handling, not a claim that factual accuracy is solved.

Validation: 88 native summary tests passed, including real HTTP request serialization, stopping before final synthesis after a failed second part, and rejecting a wrong action owner even when the correct name appears elsewhere in the report. The initial `20260905-1702-summary-quality` build was blocked from activation while the Mac was locked. It was activated on September 8 and then superseded by the verified workspace build above. The standalone tester remains unchanged.

Nine synthetic source cases and the opt-in real-model harness are tracked in `frontend/tests/fixtures/summary-quality.json` and `frontend/src-tauri/src/summary/quality_evals.rs`. Run with `AFTERWORD_EVAL_MODEL=gemma4:e4b-mlx node frontend/scripts/eval-summary-quality.cjs`; the script imports the production frontend source wrapper and defaults to runtime context sizing. Reports default to `/private/tmp/meetnola-summary-quality.json`; `AFTERWORD_EVAL_REPORT` overrides this. Checks now require owners, deadlines, and decisions in the correct section, with both bold and ATX Markdown headings supported. Cases include conditional offers, reassigned work, negative wording in confirmed tasks, and unassigned typed notes. These are bounded regressions; manually inspect the prose for omissions and unsupported claims.

The sparse-title regression now rejects inventing implementation or development work from a preservation check, while accepting a faithful checklist restatement. A September 8 comparison using all nine cases rejected a concrete-example prompt: it stopped the invented investigation for an unassigned issue but dropped Lee's confirmed task and the non-Latin detail. The prompt and template changes were fully reverted; the active dev build and model selection were unchanged. The baseline also failed budget-status and corrected-deadline checks. This pass strengthens evaluation only; 89 native summary regressions passed, with the live-model evaluation excluded from that unit count.

Accuracy remains open. Final 4B output misclassified an unapproved budget as rejected and omitted the assurance that existing records were intact. Final 9B output passed four of five cases, but promoted an explicitly uncommitted offer into an action item. Its five short-case calls took 1.48–3.88 seconds; this is not a long-meeting benchmark. Low reasoning was abandoned after the five-case run exceeded three minutes. Higher-precision `qwen3.5:4b-mxfp8` and `qwen3.5:9b-mxfp8` were downloaded for comparison; neither was selected in the app. Keep current local Parakeet and `qwen3.5:4b-mlx` selection until a candidate clears factual review. The source-coverage fixes are now active; factual accuracy remains the main unresolved enhancement issue.

Additional local experiments were rejected: a full editing pass introduced new detail; a constrained action reviewer removed a confirmed task using evidence about a different proposal; structured task extraction omitted committed work; neutral presence-penalty sampling did not fix assignment or owner errors. The installed 35B model also generated the uncommitted task with reasoning disabled. None of these review stages or sampling experiments was enabled in production, and no model selection changed. The follow-up commit strengthens regression coverage only. The Mac was still locked at the end of that September 5 evaluation; native UI checks resumed on September 8.

## Granola comparison: meeting browsing and note workspace

Inspected the running Granola Home, saved note, and enhanced-note selector through native UI without editing Granola data. Adopted its date-grouped meeting list, restrained reading width, and always-reachable meeting composer. Keep Meetnola's white shell and local providers. Calendar integration, shared workspaces, and cross-meeting chat are outside this batch; they need separate data and product decisions.

| Existing behavior | Decision and result |
| --- | --- |
| Home navigation, title search, all/recent views, draft, recovery, and import | Preserve; show search on Home, group by local calendar date, use compact rows, and disclose system details below the list. Recovery/device problems keep details expanded. |
| Saved original/enhanced notes, title autosave, explicit enhanced-note Save | Preserve; narrow the reading column, align editor text with the title, and remove the redundant Saved meeting label. |
| Meeting questions, recipes, Markdown answers, and clear | Preserve; keep a compact input dock visible while the document scrolls. Enter submits; Escape collapses answers and restores input focus. |
| Transcript timestamps, pagination, refresh, and copy | Preserve; add case-insensitive phrase search with highlighting, count, empty state, clear, and retry. Active search loads the complete transcript through the existing native API when the page is only partially loaded. Closing the sheet restores focus to Transcript. |

Passed: TypeScript, 36 workflow regression tests, three markdown tests, production frontend build, and diff validation. New regression coverage includes date/year boundaries and invalid dates, literal punctuation/HTML-safe highlights, later-page matches, retry after read failure, and stale-response isolation. Native checks covered Home search across all 45 meetings, no-match/clear, draft entry without recording, highlighted transcript matches/timestamps, long enhanced-note scrolling with the dock visible, a grounded local Qwen answer, and Escape focus return. Captures were 768 × 966, 568 × 777, and 1024 × 768 (scaled desktop captures, not measured CSS viewports). Dense summary tables keep local horizontal scrolling at compact widths; the page and assistant remain usable.

Recording and note persistence logic are unchanged. The known sparse-source summary grounding issue remains open. Dev hot reload is restored on port 3118; native build identity remains `20260905-1612-fast-qwen`. No standalone package rebuild or push in this batch. Work stays on local `main`.

## Uniform surfaces and faster local Qwen

The shell, Home, Settings, loading screen, and shared document page now use the existing white `bg-background` token; the native window background is white too. This supersedes the earlier warm-center treatment and removes the sidebar/title-bar mismatch. Navigation, editing, and semantic model-selection colors are preserved. Native Settings, Home, and saved-note views were checked after hot reload and native restart.

Downloaded and selected `qwen3.5:4b-mlx` (4.0 GB download, NVFP4) in the existing Ollama provider. Selection persisted after restart. A synthetic three-bullet prompt through the app's OpenAI-compatible API took 45.02 seconds with default reasoning and 0.53 seconds with reasoning disabled; the existing `qwen3.6:35b-mlx` took 16.26 and 0.80 seconds respectively. These are individual short-prompt observations, not controlled throughput or long-meeting benchmarks. The shared native client now disables reasoning for canonical Ollama Qwen 3.5/3.6 requests. The in-app enhancement completed within the 9.4-second observation interval, but expanded sparse source notes with unsupported detail; summary grounding remains a quality follow-up, not a passed check. A focused in-app question correctly answered that the custom meeting title must be preserved.

TypeScript, all 31 frontend workflow tests, the native provider/request serialization regression, native dev build, and strict ad-hoc signature verification passed. Regenerated stale Tauri dev-cache artifacts that referenced the removed recovery worktree. Current dev build is `20260905-1612-fast-qwen` in `/private/tmp/meetnola Dev.app`, with hot reload on port 3118. Local Parakeet is unchanged and recording is stopped. The standalone tester has not been rebuilt. Work remains on local `main`; nothing was pushed.

## Visual consistency pass

Scope: address the six visual-review findings using the existing editor, navigation, Radix controls, and installed Markdown renderer. Keep a warm neutral surface with compact document controls; no provider, storage, or recording redesign.

| Behavior | Decision |
| --- | --- |
| Draft, live, and saved note editing; title and note autosave | Preserve; share a flatter document surface and compact header |
| Recording entry/stop, recovery, and enhanced-note explicit save | Preserve |
| Transcript sheet, timestamps, pagination, and copy | Preserve; move the labeled opener beside document controls |
| Sidebar navigation, search, edit/delete confirmation, and keyboard access | Preserve; use one row action menu and more title space |
| Model selection, endpoints, language, and save | Preserve; clarify labels and allocate more width to the model |
| Assistant source context and requests | Preserve; render Markdown lists, links, and paragraphs in answers |

Implemented and verified on `codex/meetnola-visual-polish`: shared document styles, 240px minimum note surface (previously 420px for saved notes), visible Transcript/Enhance controls, folder overflow menu, neutral Settings styling, Meetnola branding, wider sidebar titles with one action menu, labeled provider/model fields, and Markdown assistant answers. The installed `react-markdown`/`remark-gfm` packages render answers without accepting raw HTML. Informational alerts retain their semantic color; the existing editor, transcript sheet, and enhanced-note Save behavior remain.

Passed: TypeScript, 26 workflow tests, three markdown tests, onboarding check, production frontend build, and `git diff --check`. Native dev checks covered draft entry without recording, live notes at wide and compact sizes, Stop/save/reopen, saved transcript timestamps, formatted Qwen checklists, model picker without changing selection, sidebar edit via keyboard with focus return, and delete confirmation canceled without deletion. Captures were 1024 × 768 (scaled desktop), 754 × 1012, and 591 × 850, not measured CSS viewports. Saved-note actions and the collapsed assistant now fit the initial 754 × 1012 view; at 591px capture width, toolbar actions wrap and Settings fields stack. The selected Settings tab scrolls into view. Recording is stopped; the hot-reload frontend is restored on port 3118. The packaged tester was not rebuilt, and nothing was merged or pushed.

Synthetic recording `meeting-recording-meeting-1788647839126` preserved its notes and two transcript segments at 0:29 and 0:31. This exposed a title initialization race: its custom draft/recording title reopened as the first note line. The follow-up fix makes user title edits authoritative within a recording session, ignores stale asynchronous metadata, and restores the editable native session title on reload. The note-derived fallback for untitled meetings remains unchanged.

Title persistence passed in the native dev app: **Meetnola title persistence verified** (`meeting-recording-meeting-1788649090051`) retained its custom draft title through Start, Stop, Home, and reopening, with a different first note line and one saved transcript segment at 0:45. TypeScript, all 31 workflow tests (including late initialization, UI/tray saves, reload synchronization, and a new-session reset), three markdown tests, the onboarding check, and the production frontend build passed. Recording is stopped. These frontend changes are active through hot reload; the packaged tester has not been rebuilt.

## Note workflow design batch

Scope: make drafting, recording entry, and reopened note editing predictable without changing audio, recovery, or enhancement providers.

| Behavior | Decision |
| --- | --- |
| Local draft persistence and recording seeding | Preserve; opening a draft must not start audio |
| Home, sidebar, and call-banner recording entry | Preserve explicit recording entry; distinguish it from **New note** |
| Original notes and titles after stop/reopen | Improve with the existing editor, automatic saving, visible failure/retry, and save-before-Home |
| Enhanced-note editing and explicit summary save | Preserve in this batch |
| Recovery, transcript persistence, Parakeet, and Qwen | Preserve |

Implemented on `codex/meetnola-note-workflow` and consolidated into local `main` by fast-forward through `c5a3d78` on 2026-09-05. Nothing was pushed. The existing local draft remains a single scratchpad; this batch does not introduce a separate note collection. The build badge was also moved to the bottom edge because it obscured **Start recording**.

Passed: TypeScript, 21 recording/workflow regression tests, 3 markdown tests, and native dev checks for **New note** / **Draft → Open** without recording, draft persistence, explicit recording with draft seeding, stop, post-stop editing, reopening, and immediate Home navigation after editing the reopened note/title. Synthetic evidence is **Meetnola reopened edit verified**. The build badge no longer overlaps the tested Home, recording, or saved-meeting actions. Tested at the current desktop window size; narrow layouts and packaged builds were not retested. Parakeet transcription and local Qwen enhancement settings are unchanged.

The follow-up batch adds notes-aware meeting questions/recipes, stored dates in the native meeting-list response, and **View all meetings** with title search and an empty-results recovery action. TypeScript, 22 frontend regression checks, four native quality tests, and the native dev build passed. Live checks confirmed a local Qwen answer on a notes-only meeting, all 42 meetings in the full list, matching and empty searches, clearing search, and dates displayed after restarting the rebuilt app.

Navigation accessibility is implemented: named collapsed controls, a navigation landmark, selected/expanded states, keyboard-operable meeting rows, visible focus and row actions, focused search/meeting expansion, and focus return after closing the title dialog. TypeScript and all 22 workflow regression tests passed. Native dev checks passed for Tab/Enter meeting navigation, Escape from the title dialog, Space to collapse meetings with hidden rows removed from the tab order, and search expansion/clearing. This closes the six findings from the bounded design review; a full VoiceOver audit remains outside the verified coverage. Narrow-window checks are recorded below. Enhanced-note editing still uses its explicit save action.

Current dev build: `20260905-1329-discovery` in `/private/tmp/meetnola Dev.app`, with frontend hot reload on port 3118, including the timestamp fix through `c5a3d78`. The temporary app contains its helper binaries and resources; strict ad-hoc signature verification passed. This checkout is now on local `main`; the packaged tester remains at `20260905-ae4bd26`. Earlier no-merge statements below describe the state at each validation pass.

The fully merged recovery worktree was removed on 2026-09-05. Its build cache was preserved at `/Volumes/ServerData/Apps/meetily/target/meetnola`; set `CARGO_TARGET_DIR` to that path to reuse it. The packaged tester is now at `target/meetnola/release/bundle/macos/meetnola Tester.app`, and strict signature verification passed after relocation. The existing main-checkout cache and running dev app were preserved. Recovery-worktree paths below are historical.

## Compact-window and package verification

Scope: preserve navigation, note saving, model choices, and page actions; improve shared content width, title wrapping, and compact AI controls. No navigation or editor replacement.

| Behavior | Decision |
| --- | --- |
| Sidebar collapse, page navigation, Home discovery, Settings tabs | Preserve; remove duplicate content margin and contain tab scrolling |
| Title autosave and editing | Preserve; recalculate title height on width changes |
| AI follow-up recipes and source context | Preserve; enable the Send button for notes-only context as Enter already does |
| Sticky AI panel | Preserve at large widths; use normal document flow at compact widths to avoid covering notes |

Native dev checks passed at an 834 × 768 capture and a compact 556 × 812 capture (window-image dimensions, not a measured CSS viewport). Before: expanded navigation left an extra 256px margin plus 32px padding in `MainContent`; narrow titles clipped, the sticky AI panel covered note text, and Settings tabs widened the page. After: the duplicate 288px spacing is gone, titles wrap when resizing/collapsing the sidebar, compact controls scroll below the editor, and keyboard End reaches Beta within the scrolling tab strip. Home and **New note** fit; the draft stays **Not recording**. The notes-only **Send question** button returned “Autosave was verified” from local Qwen on the synthetic reopened-edit record.

Passed: TypeScript, 22 workflow regression tests, three markdown tests, the onboarding check, production frontend/native release builds, and strict ad-hoc signature verification. Packaged tester `20260905-ae4bd26` is at `/Volumes/ServerData/Apps/meetily-recovery/target/release/bundle/macos/meetnola Tester.app`. It ran from `tauri://localhost` with the dev server stopped. Packaged checks passed for draft entry without recording, all 42 meetings with dates, compact layout, and editing the synthetic **Meetnola reopened edit verified** note followed by immediate Home navigation and reopening. Its text is now “Synthetic packaged edit. Autosave verified.” Audio capture was not repeated; Home audio-device readiness still displayed **Checking…** during this pass, so capture readiness is not newly verified. The packaged app was closed and the existing hot-reload dev workflow restored. No merge or push. This is compact desktop validation, not a mobile-support, notarization, or full accessibility claim.

Audio retry at 14:52 PDT on 2026-09-05 passed in the dev app: recording started, synthetic system speech transcribed, Stop saved three segments, and the title, notes, and transcript survived reopening **Meetnola audio retry verified** (`meeting-recording-meeting-1788645074580`). Home then showed both microphone and system audio as **Default**, with 43 meetings. Recording is stopped. No code change or permission reset was needed; the earlier device-readiness observation is superseded for this dev run.

Saved transcript timestamp fix verified at 15:06 PDT: the sheet previously read `audio_start_time` from virtualized segments that expose `timestamp` instead. It now renders typed saved API transcripts directly. The same synthetic meeting displays **0:25**, **0:29**, and **0:48**, including after **Refresh** and reopening the sheet; no stored-data change was required. TypeScript, 24 workflow tests (including API-to-row rendering and missing/zero/invalid timestamp coverage), three markdown tests, and the onboarding check passed. The fix is active through dev hot reload; the packaged tester has not been rebuilt with it. No merge or push.

## Quality fixes (2026-09-05)

- Code commit `32fb2ab` on `codex/meetnola-quality-fixes` addresses the seven review findings: checkpoint retention, Unicode-safe truncation, idempotent saves, editor clearing, saved-title persistence, accurate **New recording** behavior, and notes-only enhancement.
- Packaged tester build `20260905-32fb2ab` replaces the earlier bundle at the recovery worktree's app path. Local `main` was fast-forwarded through `3aee9b4`, which also refreshes Home's meeting list and finishes pending note/title saves before returning Home. Nothing was pushed.
- Passed: TypeScript, 16 frontend regression checks, 3 Rust tests using Unicode inputs and isolated SQLite databases, markdown/onboarding checks, production frontend/native builds, and ad-hoc signature verification. Dependencies were synchronized to the existing lockfile.
- Browser verification used the actual application editor: **Clear** emptied its visible document and stored state; subsequent typing did not restore old text.
- Earlier native retesting waited inside CoreAudio during CPAL input-device discovery. After the user's audio approval, two dev recordings started and stopped successfully. Native **Clear**, replacement notes, post-stop title edits, immediate Home navigation, and reopening passed. Synthetic audio produced two saved transcript segments; the transcript and a post-stop note edit survived reopening. TypeScript and all 16 frontend regression checks passed again after the Home fix.
- Synthetic evidence remains as **Meetnola dev quality verified** (notes only) and **Meetnola dev audio verified** (two transcript segments). No test records were deleted.
- Groq returned `expired_api_key`; the user then selected local Qwen enhancement. The tester now uses Ollama at its default local endpoint with the already installed `qwen3.6:35b-mlx` model (NVFP4, safetensors; Ollama 0.33.1). Notes-only enhancement succeeded and survived reopening, producing a summary, decision, and action item. Its generated title is **Review of Synthetic Quality Check and Parakeet Model Decisions** (formerly **Meetnola dev quality verified**). Credentials were not inspected or changed. Local Parakeet remains selected for transcription.
- The earlier quality pass used dev badge `20260905-1244-4957e34` with frontend code through `3aee9b4`. The newer running dev build is recorded above. This session uses a standalone Next dev server, so Rust changes require rebuilding and relaunching the native app. That earlier packaged release has been superseded by the design build recorded above.

## Verified recovery baseline

Afterword is maintained as an independent fork with selective upstream adoption. See [Afterword maintenance](../afterword-maintenance.md) for branch policy, supported providers, and validation commands.

- Recovery code: `54e3af9` on `meetnola/recovery-baseline`, including recording/note fixes in `7347b94`.
- Final packaged tester: `meetnola Tester v0.4.0 (bundle, 20260905-54e3af9)` at `target/release/bundle/macos/meetnola Tester.app` in the recovery worktree.
- Local Parakeet Compact remains selected by user choice. System-audio permission was granted by the user.
- Validation passed: TypeScript, 10 recording/updater regression tests, 3 markdown tests, onboarding model checks, Rust check, production frontend build, native app bundle, and ad-hoc signature verification.
- Packaged UI validation: synthetic speech transcribed; title, notes, and transcript survived navigation, stop, and reopening. A second recording on the final build saved two transcript segments; editing its note after stop persisted when reopened.
- Synthetic evidence remains in the tester as **Meetnola recovery smoke test** and **Meetnola final build verification**. No test records were deleted.
- Upstream automatic updates are disabled for Meetnola. Cloud transcription is unsupported in this baseline. Live summary-provider behavior and notarized distribution were not validated.
- Local promotion uses a fast-forward into `main`; remote publication is separate and has not been performed.

## Historical product and packaging snapshot (2026-03-09)

The sections below preserve the earlier handoff. Build versions and outstanding-work statements below are historical; the verified baseline above takes precedence.

## Purpose

This handoff captures the latest product, packaging, routing, UI, and macOS testing work that is not fully reflected in the longer-lived docs yet.

## Current packaged tester build

- Product name: `Afterword` (the build recorded below predates the rename and was named `meetnola Tester`)
- Bundle id: `com.meetnola.tester` (unchanged by the rename)
- Packaged app path: `target/release/bundle/macos/Afterword.app`
- Tester app data folder: `~/Library/Application Support/com.meetnola.tester/`
- Tester DB: `~/Library/Application Support/com.meetnola.tester/meeting_minutes.sqlite`
- The latest packaged build used during this pass showed an on-screen build badge:
  - `meetnola Tester v0.3.2 (bundle, 20260309-0932-c796cf6)`

## Why packaged builds matter on macOS

- `tauri dev` is not trustworthy for macOS system-audio permission testing.
- TCC / System Audio Recording permission attaches to the packaged bundle identity, not the transient dev binary.
- The packaged tester app is the correct target for:
  - system audio permission prompts
  - system audio recording validation
  - startup timing validation
  - peer testing

## Afterword-specific build and run paths

- Dev tester app:
  - `cd frontend && ./run-afterword.sh`
  - or `cd frontend && pnpm run tauri:dev:afterword`
- Packaged tester app:
  - `cd frontend && ./build-afterword.sh`
  - or `cd frontend && pnpm run tauri:build:afterword`
- Tauri config override:
  - `frontend/src-tauri/tauri.afterword.tester.conf.json`
- `pnpm run tauri:dev:meetnola` / `tauri:build:meetnola` still exist as thin aliases.

## Recording entry path is unified

These now intentionally go through the same route and should no longer be treated as separate features:

- Home `Start recording`
- Sidebar `Start Recording`
- Call-detection banner entry

Shared routing helper:

- `frontend/src/lib/quickNoteRoute.ts`

Expected behavior:

- New sessions route into `/recording`, which owns start/stop; reloading it attaches
  to the session still running natively instead of reverting to a draft
- Draft notes (Home `New note`) stay on `/quick-note`, which never captures audio
- Both routes render `frontend/src/app/_components/NoteWorkspace.tsx`
- There should not be a separate “sidebar recording page” behavior anymore
- The `?fresh=<ts>` sessionStorage start token is gone; recording intent lives in the path

## Saved-note / saved-meeting UX model

The product is moving toward a Granola-style hierarchy:

- Main panel is document-first
- Transcript is hidden by default and opens from the wave button
- AI follow-up composer lives at the bottom
- If no AI summary exists yet, the main CTA is `Enhance notes`

Primary files:

- Quick note saved view:
  - `frontend/src/app/_components/NoteWorkspace.tsx` (rendered by `/quick-note` and `/recording`)
- Saved meeting details view:
  - `frontend/src/app/meeting-details/page-content.tsx`
- CTA:
  - `frontend/src/components/EnhanceNotesCta.tsx`
- Notes-aware summary prompt:
  - `frontend/src/lib/enhanceNotes.ts`

Current state:

- The hierarchy is flatter and quieter than before
- The saved quick-note stop screen and reopened meeting screen are closer than before, but still not fully shared
- Both now support a top-right overflow action surface instead of diverging on basic meeting actions
- Saved-note bottom controls are anchored more like Granola and the transcript control uses a waveform-style glyph
- Remaining work is mostly visual compression and shared-shell extraction, not route architecture

## Notes-driven title fallback

Meeting titles are no longer dependent only on transcript/summary output.

Current behavior:

- If a meeting still has a generated or placeholder title such as `Meeting 2026-...` or `New note`
- and the saved notes contain meaningful content
- the app derives a title from the first useful note line and persists it

This now happens through the native note-save path, not only through summary generation.

Key files:

- `frontend/src-tauri/src/notes_commands.rs`
- `frontend/src/lib/suggestMeetingTitle.ts`
- `frontend/src/app/quick-note/page.tsx`
- `frontend/src/app/meeting-details/page-content.tsx`

## Build identity

Tester builds now expose visible build identity in-app so it is possible to confirm which bundle is open.

Files:

- `frontend/src-tauri/build.rs`
- `frontend/scripts/tauri-auto.js`
- `frontend/src-tauri/src/lib.rs`
- `frontend/src/lib/buildInfo.ts`
- `frontend/src/components/BuildIdentityBadge.tsx`
- `frontend/src/components/About.tsx`
- `frontend/src/app/settings/page.tsx`

## Groq-first product defaults

Fresh installs were changed toward Groq-first behavior:

- Summary default: `groq`
- Transcript default: `groq`
- Local models are optional rather than required up front
- Groq / OpenAI keys are shared across transcript + summary settings

Related files include:

- `frontend/src-tauri/src/config.rs`
- `frontend/src-tauri/src/database/commands.rs`
- `frontend/src-tauri/src/database/repositories/setting.rs`
- `frontend/src/contexts/ConfigContext.tsx`
- `frontend/src/components/ModelSettingsModal.tsx`
- `frontend/src/components/TranscriptSettings.tsx`
- onboarding flow under `frontend/src/components/onboarding/`

## macOS system-audio reality

Current macOS behavior is more nuanced than the older docs suggest:

- Default backend is `Core Audio`
- `ScreenCaptureKit` remains available but is intended for loopback-style devices
- `ScreenCaptureKit` should not be treated as the default path for normal playback-output capture
- Packaged app permission testing is required for trustworthy macOS system-audio validation

Key files:

- `frontend/src-tauri/src/audio/capture/backend_config.rs`
- `frontend/src-tauri/src/audio/recording_preferences.rs`
- `frontend/src/components/AudioBackendSelector.tsx`
- `frontend/src-tauri/src/audio/capture/core_audio.rs`
- `frontend/src-tauri/src/audio/stream.rs`

## Startup / launch behavior

- The app now uses build identity and a more stable startup path than earlier in the session
- `clean_run.sh` is improved, but packaged builds remain the correct validation path for peer testing
- The DMG bundling step may still fail even when the `.app` bundle succeeds

Practical implication:

- For local validation, the `.app` bundle is sufficient
- DMG failure does not necessarily mean the app bundle is invalid

## Tester distribution / launch reality

The tester README is now the operational source for peer setup:

- `docs/afterword-tester-readme.md`

Important current facts:

- The `.app` bundle works
- The `.dmg` step still fails
- The app is ad-hoc signed, not notarized
- Some transfer/extraction paths may strip executable permissions from binaries inside the bundle
- `frontend/build-gpu.sh` now normalizes executable bits on `Contents/MacOS/*` after build, even if DMG bundling fails later

If a tester reports launch failures like error `111`, `permission denied`, or `Launch failed`, the README now includes:

- `chmod +x .../Contents/MacOS/meetily`
- `chmod +x .../Contents/MacOS/ffmpeg`
- `chmod +x .../Contents/MacOS/llama-helper`
- quarantine removal with `xattr -r -d com.apple.quarantine`

## Key docs already present in `docs/wip/`

- `logging-overhaul-plan.md`
- `recording-transcription-stability-plan.md`
- `handoff-logging-stability-phase1.md`
- `handoff-logging-stability-followup.md`
- `handoff-ui-stuck-recovery.md`
- `handoff-recording-ui-churn-fix.md`
- `handoff-groq-first-defaults.md`
- `handoff-meetnola-tester-build.md`
- `handoff-macos-system-audio-restore.md`
- `handoff-macos-audio-backend-followup.md`

## Recommended next steps

1. Continue visual simplification of saved-note / saved-meeting surfaces.
2. Extract a shared saved-meeting shell if quick-note and meeting-details continue to drift.
3. Keep validating macOS system audio only in the packaged `Afterword.app`.
4. Fix remaining macOS branding leakage (`Meetily` identity in menu bar / app switcher).
